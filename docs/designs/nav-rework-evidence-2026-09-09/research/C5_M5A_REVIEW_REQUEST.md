# C5 m5a attribution review

C3 rejection accepted; root restored only primary C3 source/viewer and retained the new test. C8 frozen snapshots independently read and native m8i mechanism/quality run launched; no outcome yet. Keep C7 implementation paused.

PROCEED documents-only: review downloaded C5-v2-m5a raw traces/count outputs, reconstruct first-fill vs no-fill measured tick attribution as done for m8i, verify masks/pops between arms, and write C5_M5A_REVIEW.md plus a machine-readable stage summary. Inspect all .exit statuses: build 0 and run 1 are expected timing failures, not a green gate. No source/native mutations. Explain whether packing or hit replay dominates cold fills and what remains after tick31. Check actual trace stage names and tick row markers rather than assuming prior structure. End C5 M5A REVIEW READY and wait.
