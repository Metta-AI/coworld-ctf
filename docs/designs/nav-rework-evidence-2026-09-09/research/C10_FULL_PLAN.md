# C10 full-source screen

Native micro passesbothhosts and selectsSIMD; C10_NATIVE_MICRO_RESULT.md. Root materialized an isolated full-source candidate in nav-deferred-cache/src/shell/body_nav.nim, frozen under C10/full/. Parent is retained C2+V1. The candidate imports existing SSE2/NEON bindings by compile target, uses SIMD on those targets and the exact grouped-scalar counterpart elsewhere, preserving the C2 miss path/source order/cadence. The lane mask becomes a compile-time constant and the conservative memory ledger counts its256bytes once per system, including scalar targets. No caps change.

Local first checks: nine existing cache tests and the navigation suite pass, and both explicit server compile shapes pass. The5crafted+256border full-source replay check is running against the actual replaced function, not merely the tools helper. Native full run will compare five interleaved process pairs of nine recorded source traces and four changing/repeated regimes, then3072quality. Original nativebasef9body_nav is restored on exit; both arms overlay the same retainedC2/V1parent outside replay.

Fixed screen: per-trace median candidate/parent total<=1.01 and p95<=1.03; each changing-source regime median total<=1.01; each repeated-source regime median total<=0.90; exact ordered inputs/raster hashes/cache counts, exact quality hash and strictqualitythresholds. Five pairs on bothhosts were selected before the micro. No whole-body adoption before this screen and fresh integrated76mapmemory/configuredtickqualification; minimum5%worstconfiguredp95gain before retention, absolute finalgates unchanged. Fullsource changing-regime regressions still reject even if the micro is fast.

Peer: after C11 count work and micro-result verification, review only the diff C10/full/parent-body_nav.nim -> candidate-body_nav.nim and this runner/plan, focusing on constant-table accounting, architecture fallback and equivalence to the proven prototype. Write C10_FULL_SOURCE_REVIEW.md, end C10 FULL SOURCE REVIEW READY and wait. No implementation/nativejobs. Root owns them. Primary production source remains unchanged.

Full-source replay correctness now passes fivecrafted+256border cases against the actual function. Navigation17/17 and cache9/9 suites pass. Native quality is compared to frozen pre-H1hash5a1340213fe3046dc119d6f8d5d59d59cbfd379f885b95968f105d3edcbca500,3072scored/zero missing/illegal, all49strata p95<=0.5%andmax<=3%. Later integrated qualification must preserve retainedH1hash ee2488d32085fb4de3457c4cf841298c14b87add2225964d934f80f2a7c1013d. Full-source evaluator is frozen before measurements in C10/full/.
