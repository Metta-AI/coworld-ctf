# V1: byte visit generations, isolated measurement

Parent production source f9dff753 (A2); H1 and rejected A3 absent. Reuse the existing per-source generation stamps and full-clear rollover; change only their storage width from uint32 to uint8, allocation, maximum and retained ledger element size. No new dependency or algorithm. V1_REVIEW.md proves the existing invariant remains valid and identifies frequent rollover as the new risk. Full arrays still exceed Zen1 L2; no cache-fit or cache-miss attribution is claimed.

First add a public-API regression that rebuilds the same source over600 times with empty and duplicate-source rebuilds, comparing every result to fresh-system reference rasters. It must fail if the full clear is removed. Native m5a CPU5: three interleaved parent/candidate whole-body pairs at331 and1300px, full candidate quality, activation and configured memory diagnostics. Require unchanged quality hash, exact masks/pops for identical harness inputs, and retained workspace decrease exactly3bytes per allocated cell per seat. Preserve all timing failures. Do not adopt unless danger work improves without a material whole-body regression.

The standard120sample tick run cannot qualify wrap cost. Before adoption, run a longer identical parent/candidate diagnostic crossing multiple real rollovers, including configured and colossal rasters, recording rollover-adjacent samples separately. The focused regression is a correctness screen, not that timing proof. Any repeated-raster mismatch rejects immediately. Memory caps remain32MiBshared/256MiBtotal; no new cap increase is needed.

Test audit: the initial constant-source test passed even with clearing removed: every generation overwrote the same stamps. The corrected test leaves one separated source region untouched for255source generations, then revisits it. The missing-clear mutation now exits1; the real implementation is restored before its final focused run. Both mutation logs are retained, including the ineffective first test.
