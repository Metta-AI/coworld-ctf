# C2 integration in progress

Integrate the frozen cache algorithm onto ff64a224 (V1+A4+H1), preserving byte visit stamps and the exact-goal heuristic. Native isolated C2 short rows pass headroom on m5a; all27native recorded-trace pairs improve total/p95 with exactraster chains and model hitcounts. Changing-source total time ranges0.9918–1.0108x parent (8/12slower); repeated-source0.2215–0.3302x. The no-slowdown prediction for changing sources is falsified; retain that tradeoff. Quality/determinism/memory remain hard gates and full configured results now fail timing on all 11 maps (worst p95/max 6.484532/6.680327 ms); all 76 isolated memory rows pass.

Root review found one missing16byte allocation allowance for the sharedcache ref owner. Integration adds it consistently with the existing sharedDangerGeometry owner. Isolated C2rawledgers therefore undercount this final representation by16bytes; do not rewrite them. Finalfreshledger checks must include the correction. No change to timed cache logic from that accounting fix. Diagnostic trace benchmarking remains tools-only, using a private include; the peer's separate diagnostic srcmodule is archived but not added to production src.

Run cache/nav focused tests, bothservercompile shapes, viewer rebuild and fresh integratedquality/activation checks before accepting the combinedcandidate. Finalbudgetselection, fleetmatrix, allninefixtures and canonicalDockerqualification remainopen.

Integration regression: cache hits masked the old constant-source rollover exercise. Added an eviction/miss sequence over255distinct origins before revisiting a disjoint source region. Removing the full clear now fails this combinedcache+byte-stamp test; the real clear was restored before the positive run. The earlier nav test is renamed to claim only repeated-rebuild behavior.

Integrated checks: 8/8 cache tests, 17/17 navigation tests, both server compile shapes, Docker viewer rebuild and viewer module/stamp QA pass. The missing-clear mutation fails the eviction/rollover regression, with real source restored. Full 3,072-case quality passes with H1 hash ee2488d32085fb4de3457c4cf841298c14b87add2225964d934f80f2a7c1013d and 31,814,084 pops. Fresh integrated memory passes all 76 maps; maximum total is 229,158,347 bytes and maximum non-colossal shared bound is 32,464,268 bytes. The additional 16-byte owner allowance is included. Mac timing is informational only; configured run exits 1 on timing and is not a native gate.
