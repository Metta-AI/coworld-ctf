# G1: configured variant geometry screen

SourceS1 plus harness changes only. Read checked-in battle-royale-s2 manifest configuration and its11-map brpool16; explicit gunRange1300. Reuse existing deterministic selectPairs sampler from corpus writer for legal near/far endpoints; record coordinates and map/manifest/pool hashes. Do not regenerate frozen3072case corpus.

Initial diagnostic: one fresh complete11-map process on m8i CPU5, B1024, Nim2.2.6/prior release flags. Every map gets the existing allocation measurement and six16/32-seat first/moving/stuck rows with120samples each. Report all failures against unchanged4/5ms tick and16MiBpool/256MiBtotal memory gates. No selected budget or repeat qualification from this single screen. Follow with repeats and slower-host measurements after coverage is reviewed.

This is a checked-in configuration screen, not a hosted readback or canonical gate. Sampling endpoints uses the existing331px zero-danger route selection solely to freeze legal static workloads; timing constructs fresh episodes at configured1300px. The corpus gate remains independent and unchanged.
