# M2: count retained sequence capacity

Accounting correction only, no allocation compaction or search change. Replace sequence lengths with capacities in navigation, mixed-graph/workspace, route-index and hazard ledgers. Keep existing limits. Preserve object-existence guards and actual-seat iteration; capacity is only for allocated payload and header presence. The16-byte allocation allowance remains an estimate, not a claim about physical RSS.

Add a regression that reserves extra bridge capacity without changing its contents and requires the ledger to count it. Run all65original activation rows and all11configured maps at1300px, recording every newly exposed failure. No quality/timing improvement assumed. Prior logical-size passes remain unproven until this accounting is measured. Do not compact in M2; any compaction will be a separate experiment so the exposed slack is retained as evidence.
