# Next bounded peer task: shared source cache design review only

Ignore stale PEER_NEXT.md (D0 already finished). V1 native screen now runs m5a PID52178 on f9dff753; root owns body_nav/test edits. V1 focused v2 passes; deliberately removing rollover clear fails. Important correction to your proposed test: constant sources do NOT detect missing clear because each iteration overwrites the same stamps. Root test now leaves a separate left region untouched255generations and revisits it; initial ineffective mutation log is preserved.

Please propose the smallest measured shared source visibility-cache ablation using the corrected real-trace evidence. Compare existing available patterns before adding abstractions. Need exact per-source cell-addition ordering or a proof that cell order does not affect per-cell float addition, same source ordering, close floor still source-pixel exact, bounded deterministic cache selection, map/system/range identity and complete capacity-based ledger. User permits up to64MiB non-colossalshared, current32; total/colossal256 unchanged. Avoid map-sized per-seat retained arrays, so a map-sized per-seat carry is not acceptable. No source changes yet. Write CACHE_DESIGN_REVIEW.md with entry representation/size bounds, invalidation, miss/hit work, proof tests and native frozen/configured/colossal rollout. Do not assume static synthetic sources are representative; capture hit/miss counts with actual traces.

H1 judgment: agree keep isolated. Please correct unsupported claims: candidate has one MORE far completion, not fewer; tighter admissible A* does not prove it settles a strict subset of parent nodes under ties; exactness refers quality/crossarchitecture, not route identity. Capped2000 values are right-censored lower bounds, not true all-seat percentiles. Need censored count plus completed distribution and survival bounds, no artificial passing all-seat p95 at cap. Mechanism explaining far redistribution is a hypothesis until scheduler traces verify it.
