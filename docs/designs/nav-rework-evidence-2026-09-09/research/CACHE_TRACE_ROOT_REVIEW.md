# Root verification of the real source-reuse trace

Re-ran cache_sim.py on all nine retained raw navsrc traces. Every header/system, cadence, source-count and changed-rebuild/ray pairing invariant passes. root-verified-counts.json retains the parsed per-run results; root-reproduced-results.txt retains the simulator stdout. The corrected previous-rebuild carry model does not alter totalLRU64 counts. The three pinned distinct maps satisfy the preregistered30%ray-step removal rule on2/3maps at16seats and3/3at32seats. This is count evidence only; it does not establish a tick speedup.

The initial attribution used an ever-seen seat/key set and overstated what previous-rebuild retention could serve. Revision2corrects that model. A separate bounded C2candidate now owns nav-source-cache atf9dff753; productionroot source has no cache. CACHE_IMPL_REQUEST.md records root corrections to the proposed bitmap size, LRUeviction test, ownership and ordered trace-replay seam before implementation. The331px bitmap is904bytes, not912 as the original review states.

Documentation audit: the corrected plan/result, raw capture patch, source runners and independent reproduction provide the experiment contract. No public gameplay/API/configuration change is claimed. The capture report cleanup paragraph describes its capture-time state; this local evidence checkpoint subsequently archives its artifacts, without altering the trace worktree or publishing results. Original logs have been retained, with no service credential patterns found in a targeted scan. H1_ADOPTION_JUDGMENT.md revision2remains a peer interpretation; its mechanism is explicitly a hypothesis and H1 stays isolated.
