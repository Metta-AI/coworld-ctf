# A5: bounded activation attribution after A4

C2 source is frozen and root owns native runs/integration. Please work on activation profiling independently. You may use nav-validation-symmetry as the isolated scratch tree; its existing A4 body_route_index diff and check_body_index_validation.nim are root-owned baseline, preserve them exactly. First archive those current bytes. No root/source-cache/remote edits or commits.

Question: what remains in buildPocketConnectors after A4, especially on map48, and why does the m5aA2activationratio stillreach2.412x while m8iA4passes? Existing tools/profile_body_route_index.nim is an actualFluffy constructor profile (includeprivate source). Prepare bounded stage markers in the isolated tree to split pocket canonical mapping, initial fine targets/pixel scratch, coverage cell classification, coverage pixel scan, pending resolution, graph joins, packing/collapse. No per-pixel/per-source marker explosion; mark whole loops/stages and optionally collect inexpensive diagnostic counters separately. Preserve exact production control flow and full validator. Root will run the resulting profiler on m5a whenC2frees it. A local smoke should prove trace contains intended stages and fullconstructor completes, not gate timing.

Return exact instrumentation patch/runner, local trace and a brief list of evidence-backed next questions. Do not propose skipping validated pixels or omitting validation unless equivalence can be proven; this unit is attribution only, no optimization yet. Keep local cleanup/ownership explicit and leave raw artifacts durable.
