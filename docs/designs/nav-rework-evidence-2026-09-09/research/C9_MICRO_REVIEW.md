# C9 conversion-cost micro, conditional fallback

C7ownership-corrected snapshots are frozen and m8inativePID46892is running; m5av1continuesquality. No C7decision yet. The sequential C9counts independently matchall9traces (C9_ROOT_COUNT_CHECK.json).

PROCEED tools-only in nav-source-cache, preserving/restoring the corrected C7source. Materialize frozen C2parent only while building the new diagnostic. Keep bitmap productionrecording unchanged. Build a per-origin preallocated list from a bitmap on a simulatedfirsthit, simultaneously performing that bitmap replay; collect nonzero entries in row-major bitmaporder. One add percellpersource makes this exact regardless of C7rayorder. Compare three operations: ordinaryC2bitmapreplay, firsthit conversion+replay, subsequentlistreplay. No ref-owning local inside per-cell helpers (check generatedC). No sourcecachepolicy or productioncap change.

Use v2sampling/precomputedorigincells and alternate all3timed arms acrossbatches. Clear/hash outside timing, listlength reset outside timedconversion, one preallocatedcapacity fromactualnonzerokernel count. Include4maps:5120(configuredworst),5204,5263,5001(the weakest16seattraces) at331/1300px. Build actualmaterializedparent, prove nonzeroindexset/weights andperbatchrasterhashes exact, record fullorigins/counts/rawtimes. Root runsnativeonlyifneededafterC7correctedresult.

Preregistered native advancement screen: incrementalconversioncost (conversion_replay - bitmap_replay)/bitmap_replay <=0.30 and subsequentlist/bitmap <=0.50 on every1300pxmap; report331pxcosts under the same limits. The0.30cost bound leaves headroom under weakestfirsthit countmodel0.35atlist ratio0.50. These are count-model screen limits, not whole-body acceptance. If they fail, do not relax them or claim netbenefit; a new hypothesis needs a separate review. Fullrealtrace/miss/ledger/wholebody gates still follow anypositive micro.

No booldefines in production, no C9productionimplementation. Freeze tools/snapshots/exits/generated-Ccheck and concise C9_MICRO_READY.md. End C9 MICRO READY and wait.
