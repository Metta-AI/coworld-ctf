# A5/A6 evidence documentation audit

These experiments change only isolated source snapshots, diagnostics and reports; primary production route-index source remains A4. Verified Fluffy marker definitions, count instrumentation, native runner flags, trace summaries, exactness scope and transient accounting against the saved patches. A5_NATIVE_REVIEW.md and A6/A6_SCREEN_REPORT.md retain the corrected failed fill-cost hypothesis. A6_NATIVE_STATUS.md records the unexpected lazy improvement on m8i and the direct-arm rejection. Root compared all selected array hashes/counts and actual search-result chains independently, plus all 76 retained ledgers and the native full quality result.

No new production API, config, dependency or cap is introduced by this checkpoint. The 16,900-byte extra lazy scratch remains experimental and transient. The smaller selected-array identity tool is not described as complete byte equality of all graph structs. Native timing uses no per-read pixelStandabilityCounters; the separate count run cannot substitute for it. The Mac numbers remain informational. Full fleet acceptance and integration are pending, and the canonical design still needs the final Phase 11 refresh.
