# R1: precompute the exact cell-center sight predicate

Parent: current W2/M1/M3 working source, retained as R1-parent-body_nav.nim before editing. Candidate adds one immutable, reference-shared byte table to DangerGeometry. Each entry is precisely map.isWall(cellCenter(cell)), computed once at activation. The ray's out-of-grid refusal, arithmetic checks, sequence bounds checks, iteration order and float additions remain unchanged. This adopts the existing immutable-map precomputation pattern; no external library supplies this exact project-specific predicate more simply. Prior primary research and alternatives are recorded in RASTER_REVIEW.md and W1_RESEARCH.md.

Hypothesis: replacing repeated cross-module map calls and pixel indexing with a shared cell lookup reduces danger raster work. No timing improvement is assumed. Memory increases by one byte per grid cell plus sequence metadata and owner fields; measure full M3 categories, and retain any failures against unchanged 16 MiB shared and 256 MiB total limits.

Validation: focused body navigation and seat suites; full frozen 3,072-case corpus with exact per-case danger hashes and route hash; three interleaved parent/candidate fresh CPU5 runs on m5a at configured 1,300 px, B1,024. Require every emitted mask and pop array identical. Report raw row/repeat danger timing and whole-body p95/max; do not subtract stage percentiles. Follow with the original 331 px workload and configured giant-map screen if exactness passes. Compare activation and memory separately. No fixture regeneration or production-budget selection in this experiment.
