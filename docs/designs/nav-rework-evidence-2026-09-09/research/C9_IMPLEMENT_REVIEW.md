# C9 implementation unit: PROCEED in isolated nav-source-cache only

Both native micro screens are DONE, sources restored, all80rows exact and all16host/map/range median configurations pass. C9_NATIVE_RESULT and both host summaries are final. C9 PLAN CORRECTED READY verified; root accepts the corrected plan.

Implement the smallest frozen-C2-based first-hit-list candidate in your isolated tree only. Preserve corrected C7 snapshots; source is a new candidate, not integration. No primary src/test/viewer/cap edits, no native or hosted jobs. Keep C2 miss recording and sequential64LRU exact. Measure real object size/padding and seq capacities; preserve two owner allowances in integration guidance. Use focused regression tests as corrected, all9trace chains/counters/conversioncounts, actual measured ledger fields for all76maps. Fullquality can remain rootnative. No artificial public setter or per-cell owner local.

Freeze under research/C9/full: parent/candidatebody_nav, exactpatch, changed tests and tools, local raw checks, measuredledger, sourcehashes, generatedC function excerpts. Avoid copying unrelatedtools. Provide reproducible command list and explicit unimplemented gates. Finish C9 FULL READY and wait. If context requires compaction, write a durable peer handoff before continuing; do not restart completed experiments.

Root native follow-up preregistration: three interleaved C2/candidate actualtrace and changing/repeatedregime pairs perhost, no counters/Fluffy in timing; quality3072strict oldhash. Screen for no material regression before wholebody: each trace's three-repeat median total ratio<=1.01 and p95ratio<=1.03, each changing-regime median<=1.01, repeated-regime median<=0.90. Report every rawpair and avoid averaging away a map failure. These numeric noise allowances apply to this new C9candidate, not retroactively to C7. Then actual integrated memory<=authorized64MiBshared/256MiBtotal and m5aconfigured worstp95 improvement>=5% before retaining extra memory; finalordinary/headroomgates stillabsolute andunchanged. If you disagree with a criterion, surface before timing starts; do not silently alter it.
