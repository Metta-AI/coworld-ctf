# D1 rejected: carrying the kernel index is slower

Three interleaved native m5a CPU5 pairs at B1024/range1300: parent D0a worst body p95/max5.839953/5.887573 ms; candidate5.936474/5.996664 ms. Inclusive danger worstp95 also rises3.541643->3.627145 ms. All masks/pops match. Full m8i3072 case records, route hash and65 retained ledgers are exactly identical to D0a. Both focused suites and both server compile shapes pass.

Reject and restore D0a byte-for-byte. Maintaining an index on repeated visits was the preregistered counter-hypothesis; these measurements support rejection, but do not isolate every instruction-level cause. Candidate source, exact diff, independent review and raw timing/quality remain archived. The existing D0a viewer still matches the restored source, so no replacement bundle is needed for a rejected experiment. This is a negative result, not progress toward tick acceptance.
