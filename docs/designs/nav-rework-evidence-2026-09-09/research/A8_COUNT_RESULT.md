# A8 exact-input recurrence result

Counts-only wrapper ALWAYS executed the raw search. Scope resets at each scratch creation, retaining separate side/pocket summaries; all target refs independently decoded and asserted; zero exact-key result or dequeue mismatches. Source restored to f9dff753 in root-owned nav-early-visited. Build and run succeeded; artifacts A8/local-mac, bothsource snapshots and run_count.sh.

Map48:457calls,805625dequeues. Sides2calls936dequeues, no repeats. Pockets455calls804689dequeues,266distinctkeys,15176keypayloadbytes. Exactly189repeated empty failures account for356784dequeues (44.286610% of all search work). All189weak-key repeats also have exact ordered targets; no extra target-growth recurrence was found. Zero reached-empty or successful repeats. The pre-run corrected100call/25%dequeue screen passes. Configured3/5/6 have5/96/5calls and zero exact or weak repeats. This is an operation-count result, not a constructor-time gain.

Root source audit: cached failure can only be returned after the same validstart and beginSearch boundary, preserving generation advancement and rollover; invalid-start returns need not be cached. Complete key must preserve start,center,exactEdges and orderedtargets; map immutable and scope scratch-local. No approximate hashes or target-set containment assumption.

Peer bounded next task (documents-only): independently reviewcounts and propose the smallest reversible native speed screen on A4base. We need a clear memory accounting plan for transient cache storage. A stdTable usesexistinglibrary but hidescapacity; avoid guessed physicalcapacity in transient stats or an unnecessary customhashmap. Consider fullkeyfailure memo versus a per-start/mode latestfailedorderedtargetlist or smallboundedlinear cache; explain measured worstcase storage and overhead, and prefer existing stack. No implementation until review. Suggest native advancement criterion, retaining full76index/quality proofs and finalactivation gate. Rootowns implementation tree/native runners; peerreviews only. End A8 IMPLEMENTATION REVIEW READY andwait.
