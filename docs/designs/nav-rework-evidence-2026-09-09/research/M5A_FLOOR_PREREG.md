# M5a baseline floor follow-up

B0-m5a failed all four budgets. Measure B0, B256 and B512 to bound the
non-search floor and find whether any positive budget passes. Budget zero
is diagnostic only and cannot be selected for production. Same parent20234cc7,
Nim2.2.6, CPU5, three pinned plus one informational unpinned process each.
No concurrent benchmarks. Preserve all results. This is a new registered
measurement prompted by failure, not a retry of rejected rows.
