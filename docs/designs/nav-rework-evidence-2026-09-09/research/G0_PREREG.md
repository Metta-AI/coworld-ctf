# G0: isolate configured gun range

Source S1 checkpoint29cf46f3 plus a harness-only --tick-gun-range argument; B1024. No production code or corpus edit. Same binary on m5a CPU5, three interleaved fresh331/1300process pairs; Nim2.2.6 and prior release flags. Original64-map corpus map15/endpoints/scenarios unchanged. Record six whole-body rows, danger/weight/weapon slices and per-tick pops. Different gun ranges intentionally change danger, so equal routes/pops are not expected across arms.

This isolates range only; current-manifest11-map geometry is a separate follow-up. Neither arm establishes production qualification. Report every result, including constructor/memory failure; never drop the old corpus or relax gates.
