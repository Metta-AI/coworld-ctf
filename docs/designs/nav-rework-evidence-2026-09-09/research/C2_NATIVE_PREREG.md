# C2 native comparison

Frozen f9dff753 A2 parent; isolated C2only, noV1/H1/A4. m5aCPU5, separate native builds with each arm's matching harness ledger. Candidate full3072quality hash must match parent beforetiming; threeinterleaved short wholebody pairs at331/1300px, then fullactivation and11configuredmapdiagnostic. No cachecounterdefine in wholebodytiming. Record all gate failures and same masks/pops checks. This first native screen does not alone qualify the cache: require native realtrace hit/miss reproduction and paired realtrace/miss-only/hit-heavy timing beforeadoption. Synthetic repeated threats are especially favorable to sharing. Do not infer fleet throughput from that regime alone.

Local evidence is copied from nav-source-cache/C2 after peerhand-off:7cachetests pass, originalnav16tests pass, fullqualityexact,9realtraces matchLRUcounts and1684rasters; shard4has18replayfixturefailures, notgreen. Native timing remains unmeasured atpreregistration. Corrected harness counts the sharedcache field in JSON and sharedcap; no capraise planned.

Separate native m8iCPU5mechanism experiment: threeinterleavedparent/candidate process pairs for changing-source and repeated-source publicrebuilds on3configuredmaps+colossal, plus all9recordedtraces through the exact ordered-source seam. Counters enabled only in this diagnostic, not m5awholebody. Record actualhits/misses; changing sources are NOT assumed allmiss. Parse/hashoutside timedrebuilds; compare rasterchainhashes and identical sampletick/seat/source sequences across arms. No whole-body acceptance from these isolateddurations. run_c2_mechanisms.sh recordsrecipe.
