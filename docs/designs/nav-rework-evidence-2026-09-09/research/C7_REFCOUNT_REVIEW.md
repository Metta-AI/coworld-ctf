# C7 root finding: remove per-cell reference ownership before judging representation

Native m8i full traces complete27pairs, all exact but total ratios1.4794–1.6740, median1.5978; allp95worse. Both original runners still finish their quality/other-host work; do not mutate native checkouts.

Root inspected generated native C. Parent addVisibleCell directly accesses seat.dangerSourceCache.bits and has no local cache owner. Candidate `let cache = seat.dangerSourceCache` emits eqcopy(cache,seat.cache) per nonzero firstvisit and eqdestroy on every return (including nil cleanup on alreadyvisited/outofbounds paths), plus error cleanup. The eqcopy body explicitly calls nimIncRef and nimDecRefIsLast; eqdestroy calls nimDecRefIsLast. This ownership overhead is proven in generated C; how much of the slowdown it explains is not yet measured. The report's attribution to streaming stores alone is therefore unsupported and must be corrected to a hypothesis.

PROCEED with a separate minimal corrected candidate in isolated nav-source-cache, preserving full v1snapshots/results. In addVisibleCell only, remove the local ref-owning alias and access seat.dangerSourceCache directly for the few field reads/writes, as C2 did. Keep scalar slot/base locals if useful. No pointers, casts, unchecked pragmas, new lifetime abstraction, cache-policy change, memory-cap change or other optimization. Keep replayVisibleCells unchanged (its once-per-hit owner is a separate question, not per-cell).

Build the actual materialized corrected snapshot and inspect generated C to prove addVisibleCell no longer creates/copies/destroys a DangerSourceCache local. Run corrected focused10cache/16nav suites and9tracechains; preserve allrasters/counts. Write C7_REFCOUNT_READY.md and parent-v1/corrected snapshots+patch+sourcehashes+Csnippet. Root will run new C2-versus-corrected pairs and separately compare v1miss totals; targetno materialmiss regression and actualtracebenefit remains unchanged. End C7 REFCOUNT READY and wait.

C9 stays counts-only pending this cleaner test. Also record C9limitations: completed and trace-end-resident lifetimes differ; say no observed hit before eviction or traceend. Secondhit is not uniformly safer (smoke allowance0.33 vs firsthit0.42). C5is synthetic configuredcoldfill attribution, not proof that real-play danger is a minority of the tick. Countscript trace labels should support both per-episode/navsrc.txt and named*.navsrc.txt files and retain sourcepath/hash.
