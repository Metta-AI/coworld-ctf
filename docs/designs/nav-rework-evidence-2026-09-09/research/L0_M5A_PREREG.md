# L0 m5a replication

Same L0 source and harness as c6a. Immediate parent is M0: ring131072 and
otherwise original body_nav. Three interleaved parent/candidate B1024 processes
on CPU5, Nim2.2.6, followed by two fresh candidate latency processes.
Record all failures and all row metrics. No change to workloads, sample counts,
thresholds or wave cap. No other benchmark runs on the m5a concurrently.
