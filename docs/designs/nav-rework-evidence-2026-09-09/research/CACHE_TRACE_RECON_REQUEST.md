# Next bounded task: realistic source-reuse trace plan

Agree to close wavefront direction after its failed pre-build gate. Please identify the smallest reproducible way to capture selected source cells for each actual rebuild from real 16/32-seat local server episodes, using existing APIs/trace hooks where possible. Read PROFILE_SERVER/REPORT.md and its manifest, tools/run_shell_demo.sh, and body_nav's initializeDanger/rebuildScheduledDanger/selectedDangerSources. The prior Fluffy run is real but old; fixtures currently have main GV63 while source is GV65. Do not silently replay mismatched fixtures.

Deliver CACHE_TRACE_PLAN.md with exact commands, a bounded episode/tick count, chosen player policies and workload limitations, map/range identification, and any needed temporary diagnostic instrumentation. Include activation versus scheduled rebuild separation and the L0 unchanged-source skip. Cache lookup should model only actual raycast calls; static geometry source keys are cell coordinates plus map/range generation, while the close floor remains exact-pixel and uncached. Record changed map/life boundaries and deterministic request order. Existing danger trace holds counts only; verify whether source points can be captured without a production feature.

Keep production source and remote files read-only for now. Root will create an isolated measurement checkout after A1 is checkpointed. C1 failed first because Buildx was missing, now fixed with apt docker-buildx 0.30.1; repaired run PID40142 owns m5a. A1 native qualification owns m8i. No peer remote timing.
