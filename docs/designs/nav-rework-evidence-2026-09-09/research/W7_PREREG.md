# W7: skip provably clear samples using existing map clearance

Parent W6, common R1/M3 state; snapshot W7-parent-body_map.nim before editing. Reuse the immutable saturated Chebyshev clearance field already constructed alongside the wall raster. At sample i, clearance d=0 blocks. For d>0, samples i+1 through i+d-1 cannot be walls because the nearest/ties-even discrete ray moves at most one pixel per axis per sample. Jump to i+d, or return clear if all remaining samples are covered. Preserve endpoint refusal and zero-length behavior. Keep all compiler checks.

Research: Hart's safe-distance stepping (https://graphics.stanford.edu/courses/cs348b-20-spring-content/uploads/hart.pdf) and the official SciPy chessboard-transform definition (https://docs.scipy.org/doc/scipy/reference/generated/scipy.ndimage.distance_transform_cdt.html). The discrete proof and independent review are CLEARANCE_SKIP_REVIEW.md. The existing map transform supplies the required bound; no external library, extra table or cache is needed. This is an application of a known distance-bound method, not a new line-of-sight definition.

Maintain the exact integer remainder state. At clearance1 use the existing incremental correction. Larger jumps use explicit int64 multiplication and floor division, including negative slopes; no tuned cutoff. Add saturation, exact-distance wall and boundary differential tests. A standalone diagnostic checks wall/zero equivalence and the eight-neighbor 1-Lipschitz property on every pixel of all64 frozen maps, all11 configured maps and colossal; this proves the measured clearance cannot overestimate wall distance without scanning every clearance square. This is correctness validation, not a timing benchmark.

After focused tests, run three interleaved native m5a CPU5 parent/candidate processes, B1,024/range1,300. Require identical masks and pops on all six rows. Full frozen3,072 quality and M3 activation remain required; keep configured-map failures visible. Report weapon and whole-body p95/max per row/repeat and all negative outcomes. No gain assumed. If results support retention, compare331px and then reevaluate actual hardware budgets; no budget change in this unit.
