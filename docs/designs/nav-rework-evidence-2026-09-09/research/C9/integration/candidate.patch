diff --git a/src/shell/body_nav.nim b/src/shell/body_nav.nim
index e509e644..d8ba62cf 100644
--- a/src/shell/body_nav.nim
+++ b/src/shell/body_nav.nim
@@ -35,8 +35,15 @@ type
     visited: seq[uint8]
     visitGeneration: uint8
 
+  DangerSourceEntry = object
+    ## One nonzero kernel add of a cached source: absolute raster index and
+    ## the exact weight. 8 bytes, no padding.
+    gridIndex: int32
+    weight: float32
+
   DangerSourceCacheSlot = object
     key: int32      ## origin 8 px cell index, -1 when empty
+    listLength: int32  ## entries materialized for this slot; -1 = not yet
     lastUse: uint64
 
   DangerSourceCache = ref object
@@ -44,13 +51,20 @@ type
     ## kernel box. The visible set of an origin depends only on the immutable
     ## sight geometry and live range, so entries never invalidate; the cache
     ## lives and dies with its nav system. One shared seq holds every slot's
-    ## words back to back (no per-entry allocations).
+    ## words back to back (no per-entry allocations). Misses record the
+    ## bitmap exactly as before; a slot's list of nonzero (index, weight)
+    ## entries is materialized on its first hit from the bitmap and replayed
+    ## on later hits. Slot reuse resets the list to "not materialized".
+    ## Each slot's list holds at most `capacity` entries, the nonzero-kernel
+    ## count: a source records each kernel index at most once.
     words: int      ## uint64 words per entry
     bits: seq[uint64]
+    capacity: int   ## list entries reserved per slot
+    entries: seq[DangerSourceEntry]
     slots: array[DangerSourceCacheSlots, DangerSourceCacheSlot]
     clock: uint64
     when defined(dangerSourceCacheCounters):
-      hits*, misses*: int
+      hits*, misses*, conversions*: int
 
   DangerCandidate* = object
     seatIndex*: int
@@ -235,10 +249,13 @@ proc retainedNavigationBytes*(system: BodyNavSystem): BodyNavigationBytes =
         result.allocatorOverhead += SequenceAllocationOverhead
     let sourceCache = system.seats[0].dangerSourceCache
     result.sharedDangerSourceCache = int64(sizeof(sourceCache[])) +
-      int64(sourceCache.bits.capacity * sizeof(uint64))
+      int64(sourceCache.bits.capacity * sizeof(uint64)) +
+      int64(sourceCache.entries.capacity * sizeof(DangerSourceEntry))
     result.allocatorOverhead += SequenceAllocationOverhead
     if sourceCache.bits.capacity > 0:
       result.allocatorOverhead += SequenceAllocationOverhead
+    if sourceCache.entries.capacity > 0:
+      result.allocatorOverhead += SequenceAllocationOverhead
   result.total = result.systemOwner + result.routeIndex +
     result.safetyScratch + result.hazardOverlay + result.safeCache +
     result.mixedGraph + result.seatOwners +
@@ -311,8 +328,13 @@ proc initDangerSourceCache(geometry: DangerGeometry): DangerSourceCache =
   let diameter = geometry.radius * 2 + 1
   result.words = (diameter * diameter + 63) div 64
   result.bits = newSeq[uint64](DangerSourceCacheSlots * result.words)
+  for value in geometry.kernel:
+    if value != 0'f32:
+      inc result.capacity
+  result.entries = newSeq[DangerSourceEntry](DangerSourceCacheSlots * result.capacity)
   for slot in result.slots.mitems:
     slot.key = -1
+    slot.listLength = -1
 
 proc newBodyNavSystem*(map: BodyMap, seatCount, liveGunRangePx: int,
                        dangerK = DangerCadenceK,
@@ -519,27 +541,44 @@ proc sourceCacheVictim(cache: DangerSourceCache): int =
     if cache.slots[slot].lastUse < cache.slots[result].lastUse:
       result = slot
 
-proc replayVisibleCells(seat: BodyNavSeat, origin: BodyPoint, base: int) =
-  ## Hit path: the same kernel adds the ray walk would make for this origin,
-  ## in box row-major order instead of ray order. Exact because every cell
-  ## receives at most one kernel add per source and cells are independent.
-  ## Recorded cells were in-grid for this exact origin, so no bounds check.
+proc materializeVisibleCells(seat: BodyNavSeat, origin: BodyPoint, slot: int) =
+  ## First hit of a slot: the row-major bitmap replay (the same kernel adds
+  ## the ray walk made, box row-major instead of ray order; exact because
+  ## every cell receives at most one add per source), additionally recording
+  ## each nonzero add as a list entry for later hits. Read the cache through
+  ## the seat field to avoid per-cell reference ownership work.
   let
-    cache = seat.dangerSourceCache
     radius = seat.dangerGeometry.radius
     diameter = radius * 2 + 1
     gridW = seat.danger.gridW
-  for wordIndex in 0 ..< cache.words:
-    var word = cache.bits[base + wordIndex]
+    base = slot * seat.dangerSourceCache.words
+    entryBase = slot * seat.dangerSourceCache.capacity
+  var count = 0
+  for wordIndex in 0 ..< seat.dangerSourceCache.words:
+    var word = seat.dangerSourceCache.bits[base + wordIndex]
     while word != 0:
       let kernelIndex = wordIndex * 64 + countTrailingZeroBits(word)
       let kernelY = kernelIndex div diameter
       let kernelX = kernelIndex - kernelY * diameter
-      let gx = origin.x - radius + kernelX
-      let gy = origin.y - radius + kernelY
-      seat.danger.values[gy * gridW + gx] +=
-        seat.dangerGeometry.kernel[kernelIndex]
+      let gridIndex = (origin.y - radius + kernelY) * gridW +
+        origin.x - radius + kernelX
+      let weight = seat.dangerGeometry.kernel[kernelIndex]
+      seat.danger.values[gridIndex] += weight
+      if weight != 0'f32:
+        seat.dangerSourceCache.entries[entryBase + count] =
+          DangerSourceEntry(gridIndex: int32(gridIndex), weight: weight)
+        inc count
       word = word and (word - 1)
+  seat.dangerSourceCache.slots[slot].listLength = int32(count)
+
+proc replayListEntries(seat: BodyNavSeat, slot: int) =
+  ## Later hits: the materialized nonzero adds, in list order. Zero-weight
+  ## cells were adds of +0.0 (exact identity on this nonnegative finite
+  ## raster) and are absent from the list.
+  let base = slot * seat.dangerSourceCache.capacity
+  for offset in base ..< base + seat.dangerSourceCache.slots[slot].listLength.int:
+    let entry = seat.dangerSourceCache.entries[offset]
+    seat.danger.values[entry.gridIndex] += entry.weight
 
 proc rebuildDangerFromPoints(seat: BodyNavSeat, map: BodyMap,
                              sources: openArray[BodyPoint], tick: int) =
@@ -557,12 +596,18 @@ proc rebuildDangerFromPoints(seat: BodyNavSeat, map: BodyMap,
       when defined(dangerSourceCacheCounters):
         inc cache.hits
       cache.slots[slot].lastUse = cache.clock
-      seat.replayVisibleCells(origin, slot * cache.words)
+      if cache.slots[slot].listLength < 0:
+        when defined(dangerSourceCacheCounters):
+          inc cache.conversions
+        seat.materializeVisibleCells(origin, slot)
+      else:
+        seat.replayListEntries(slot)
     else:
       when defined(dangerSourceCacheCounters):
         inc cache.misses
       slot = cache.sourceCacheVictim()
-      cache.slots[slot] = DangerSourceCacheSlot(key: key, lastUse: cache.clock)
+      cache.slots[slot] = DangerSourceCacheSlot(key: key, listLength: -1,
+        lastUse: cache.clock)
       seat.dangerRecordBase = slot * cache.words
       for word in 0 ..< cache.words:
         cache.bits[seat.dangerRecordBase + word] = 0
@@ -693,10 +738,32 @@ proc dangerSourceCacheEntryBytes*(system: BodyNavSystem): int =
   ## Bytes of one visibility bitmap at this system's live range.
   system.seats[0].dangerSourceCache.words * sizeof(uint64)
 
+proc dangerSourceCacheCapacity*(system: BodyNavSystem): int =
+  ## List entries reserved per slot: the nonzero-kernel count at this range.
+  system.seats[0].dangerSourceCache.capacity
+
+proc dangerSourceCacheListBytes*(system: BodyNavSystem): int =
+  ## Bytes of one slot's list reservation.
+  system.seats[0].dangerSourceCache.capacity * sizeof(DangerSourceEntry)
+
+proc dangerSourceCacheListLengths*(system: BodyNavSystem): seq[int] =
+  ## Diagnostic snapshot: materialized list length per occupied slot, in the
+  ## same least-to-most-recently-used order as dangerSourceCacheOrder; -1
+  ## marks a slot whose list has not been materialized.
+  var order: seq[tuple[lastUse: uint64, length: int]]
+  for slot in system.seats[0].dangerSourceCache.slots:
+    if slot.key >= 0:
+      order.add((slot.lastUse, slot.listLength.int))
+  order.sort(proc(a, b: tuple[lastUse: uint64, length: int]): int =
+    cmp(a.lastUse, b.lastUse))
+  for entry in order:
+    result.add entry.length
+
 when defined(dangerSourceCacheCounters):
-  proc dangerSourceCacheCounters*(system: BodyNavSystem): tuple[hits, misses: int] =
+  proc dangerSourceCacheCounters*(system: BodyNavSystem): tuple[hits, misses, conversions: int] =
     (system.seats[0].dangerSourceCache.hits,
-     system.seats[0].dangerSourceCache.misses)
+     system.seats[0].dangerSourceCache.misses,
+     system.seats[0].dangerSourceCache.conversions)
 
 proc initializeDanger*(system: BodyNavSystem,
                        sourcesBySeat: openArray[DangerInput],
diff --git a/tests/test_shell_body_danger_source_cache.nim b/tests/test_shell_body_danger_source_cache.nim
index 32d03e2c..83391f1f 100644
--- a/tests/test_shell_body_danger_source_cache.nim
+++ b/tests/test_shell_body_danger_source_cache.nim
@@ -1,6 +1,10 @@
-## Shared source-visibility cache (C2): exactness against the uncached ray
+## Shared source-visibility cache (C2 bitmap recording and LRU policy, C9
+## first-hit list materialization): exactness against the uncached ray
 ## walk, source order, close floor, bounded deterministic eviction, range
-## identity, and the retained ledger.
+## identity, the retained ledger, and the list-specific risks: capacity-
+## exact sources, stale lists after slot reuse, first versus later hits,
+## index mapping at map borders across supported ranges, and empty
+## selections between hits.
 
 import std/[algorithm, sequtils, unittest]
 import ../src/shell/body_danger
@@ -145,8 +149,15 @@ suite "shared danger source visibility cache":
     let map = wallMap()
     let near = newBodyNavSystem(map, 2, 331, prepareRouteQueries = false)
     let far = newBodyNavSystem(map, 2, 1300, prepareRouteQueries = false)
+    # Bitmap entries as in C2 (words times 8) plus a list reservation whose
+    # capacity is the nonzero-kernel count at the range (C8 kernel support):
+    # 5,385 cells at 331 px, 54,173 at 1300 px, 8 bytes per entry.
     check near.dangerSourceCacheEntryBytes == 904
     check far.dangerSourceCacheEntryBytes == 13_368
+    check near.dangerSourceCacheCapacity == 5_385
+    check far.dangerSourceCacheCapacity == 54_173
+    check near.dangerSourceCacheListBytes == 5_385 * 8
+    check far.dangerSourceCacheListBytes == 54_173 * 8
     let a = (x: 40, y: 30)
     near.seats[0].rebuildDanger(map, input((8, 8), [a]), 1)
     check near.dangerSourceCacheOrder.len == 1
@@ -162,10 +173,10 @@ suite "shared danger source visibility cache":
     let map = wallMap()
     let nav = newBodyNavSystem(map, 4, 1300, prepareRouteQueries = false)
     let bytes = nav.retainedNavigationBytes
-    check bytes.sharedDangerSourceCache >=
-      int64(DangerSourceCacheSlots * nav.dangerSourceCacheEntryBytes)
-    check bytes.sharedDangerSourceCache <
-      int64(DangerSourceCacheSlots * nav.dangerSourceCacheEntryBytes) + 4096
+    let payload = int64(DangerSourceCacheSlots *
+      (nav.dangerSourceCacheEntryBytes + nav.dangerSourceCacheListBytes))
+    check bytes.sharedDangerSourceCache >= payload
+    check bytes.sharedDangerSourceCache < payload + 4096
     var sum = bytes.systemOwner + bytes.routeIndex + bytes.safetyScratch +
       bytes.hazardOverlay + bytes.safeCache + bytes.mixedGraph +
       bytes.seatOwners + bytes.seatCaches + bytes.dangerRasters +
@@ -174,6 +185,117 @@ suite "shared danger source visibility cache":
       bytes.sharedDangerSourceCache + bytes.trace + bytes.allocatorOverhead
     check sum == bytes.total
 
+  test "scheduled rebuilds and empty selections leave the cache consistent":
+    let map = wallMap()
+    let nav = newBodyNavSystem(map, 2, 331, dangerK = 1)
+    let a = (x: 40, y: 30)
+    nav.initializeDanger([input((8, 8), [a]), input((8, 8), [])], 0)
+    check nav.dangerSourceCacheOrder.len == 1
+    nav.rebuildScheduledDanger(1, [input((8, 8), []), input((8, 8), [a])])
+    check nav.dangerSourceCacheOrder.len == 1
+    check floatBytes(nav.seats[1].dangerSnapshot) ==
+      floatBytes(rasterOf(map, [a], 331))
+    check nav.seats[0].dangerSnapshot.allIt(it == 0'f32)
+
+proc bigOpenMap(widthPx, heightPx: int): BodyMap =
+  var walkable = newSeq[bool](widthPx * heightPx)
+  for y in 1 ..< heightPx - 1:
+    for x in 1 ..< widthPx - 1:
+      walkable[y * widthPx + x] = true
+  newBodyMap(walkable, widthPx, heightPx, 2, @[(16, 16), (widthPx - 17, heightPx - 17)])
+
+suite "shared danger source list risks":
+  test "an all-visible source fills a slot to exactly its capacity":
+    # Open map wide enough for the whole 327-cell kernel box at 1300 px, so
+    # every nonzero kernel cell is visible from the centre: the recorded list
+    # must be exactly capacity long (no overflow, nothing dropped).
+    let map = bigOpenMap(2800, 2800)
+    let nav = newBodyNavSystem(map, 1, 1300, prepareRouteQueries = false)
+    let center = cellCenter((map.gridWidth div 2, map.gridHeight div 2))
+    nav.seats[0].rebuildDanger(map, input((8, 8), [center]), 1)   # miss: bitmap only
+    check nav.dangerSourceCacheListLengths == @[-1]
+    check nav.dangerSourceCacheCapacity == 54_173
+    let reference = newBodyNavSystem(map, 1, 1300, prepareRouteQueries = false)
+    reference.seats[0].rebuildDanger(map, input((8, 8), [center]), 1)
+    nav.seats[0].rebuildDanger(map, input((8, 8), [center]), 2)   # first hit: materializes
+    check nav.dangerSourceCacheListLengths == @[nav.dangerSourceCacheCapacity]
+    check floatBytes(nav.seats[0].dangerSnapshot) ==
+      floatBytes(reference.seats[0].dangerSnapshot)
+    nav.seats[0].rebuildDanger(map, input((8, 8), [center]), 3)   # later hit: full-length list
+    check floatBytes(nav.seats[0].dangerSnapshot) ==
+      floatBytes(reference.seats[0].dangerSnapshot)
+
+  test "a reused slot is unmaterialized until its first hit and then replays only its new list":
+    # Fill one slot with a long list, add 63 other unique origins so the
+    # cache is full with the long source as the least recently used entry,
+    # then miss on a short source: it must take exactly the long source's
+    # slot. A later hit on the short source must replay only the short
+    # list; stale entries from the long list must not be replayed.
+    let map = wallMap()
+    let range = 331
+    let nav = newBodyNavSystem(map, 1, range, prepareRouteQueries = false)
+    let long = (x: 240, y: 144)          # open centre, many visible cells
+    let short = (x: 12, y: 12)           # corner, few visible cells
+    let longKey = map.cellOf(long).y * map.gridWidth + map.cellOf(long).x
+    nav.seats[0].rebuildDanger(map, input((8, 8), [long]), 1)   # miss
+    nav.seats[0].rebuildDanger(map, input((8, 8), [long]), 1)   # first hit: materialize
+    let longLength = nav.dangerSourceCacheListLengths[^1]
+    check longLength > 0
+    for index in 0 ..< DangerSourceCacheSlots - 1:
+      let cell = (x: 3 + index mod 16, y: 3 + index div 16)   # never the long or short cell
+      nav.seats[0].rebuildDanger(map, input((8, 8),
+        [(x: cell.x * NavCell + 2, y: cell.y * NavCell + 2)]), 2 + index)
+    check nav.dangerSourceCacheOrder.len == DangerSourceCacheSlots
+    check nav.dangerSourceCacheOrder[0] == longKey                  # still cached, least recent
+    nav.seats[0].rebuildDanger(map, input((8, 8), [short]), 100)     # miss: evicts the long slot
+    check longKey notin nav.dangerSourceCacheOrder
+    check nav.dangerSourceCacheOrder.len == DangerSourceCacheSlots
+    check nav.dangerSourceCacheListLengths[^1] == -1                  # reused slot: list invalidated
+    nav.seats[0].rebuildDanger(map, input((8, 8), [short]), 101)     # first hit on the reused slot
+    let shortLength = nav.dangerSourceCacheListLengths[^1]
+    check shortLength > 0 and shortLength < longLength
+    nav.seats[0].rebuildDanger(map, input((8, 8), [short]), 102)     # later hit: list replay
+    check floatBytes(nav.seats[0].dangerSnapshot) ==
+      floatBytes(rasterOf(map, [short], range))
+
+  test "border origins map every entry to the right cell across supported ranges":
+    let map = wallMap()
+    for range in [331, 1050, 1300, 1600]:
+      let nav = newBodyNavSystem(map, 1, range, prepareRouteQueries = false)
+      let corners = [(x: 6, y: 6), (x: map.width - 7, y: 6),
+                     (x: 6, y: map.height - 7), (x: map.width - 7, y: map.height - 7),
+                     (x: map.width div 2, y: 6)]
+      for corner in corners:
+        let expected = floatBytes(rasterOf(map, [corner], range))
+        nav.seats[0].rebuildDanger(map, input((8, 8), [corner]), 1)   # miss (records bitmap)
+        check floatBytes(nav.seats[0].dangerSnapshot) == expected
+        nav.seats[0].rebuildDanger(map, input((8, 8), [corner]), 2)   # first hit (materializes)
+        check floatBytes(nav.seats[0].dangerSnapshot) == expected
+        nav.seats[0].rebuildDanger(map, input((8, 8), [corner]), 3)   # later hit (list)
+        check floatBytes(nav.seats[0].dangerSnapshot) == expected
+      check nav.dangerSourceCacheCapacity == (if range >= 1050: 54_173 else: 5_385)
+
+  test "empty selections between hits leave lists and order untouched":
+    let map = wallMap()
+    let range = 331
+    let nav = newBodyNavSystem(map, 2, range, dangerK = 1)
+    let a = (x: 40, y: 30)
+    nav.initializeDanger([input((8, 8), [a]), input((8, 8), [])], 0)   # seat 0 miss
+    nav.rebuildScheduledDanger(1, [input((8, 8), [a]), input((8, 8), [])])  # unchanged: L0 skip
+    check nav.dangerSourceCacheListLengths == @[-1]
+    nav.rebuildScheduledDanger(2, [input((8, 8), []), input((8, 8), [a])])  # seat 1 first hit
+    check nav.dangerSourceCacheListLengths.len == 1
+    check nav.dangerSourceCacheListLengths[0] > 0
+    let order = nav.dangerSourceCacheOrder
+    for tick in 3 .. 6:                                                # repeated empty selections
+      nav.rebuildScheduledDanger(tick, [input((8, 8), []), input((8, 8), [])])
+    check nav.dangerSourceCacheOrder == order
+    check nav.dangerSourceCacheListLengths.len == 1
+    nav.rebuildScheduledDanger(7, [input((8, 8), [a]), input((8, 8), [])])  # seat 0 later hit
+    check floatBytes(nav.seats[0].dangerSnapshot) ==
+      floatBytes(rasterOf(map, [a], range))
+    check nav.seats[1].dangerSnapshot.allIt(it == 0'f32)
+
   test "evicted sources remain exact across visit-generation rollover":
     let map = wallMap()
     let nav = newBodyNavSystem(map, 1, 32, prepareRouteQueries = false)
@@ -188,18 +310,6 @@ suite "shared danger source visibility cache":
       check floatBytes(nav.seats[0].dangerSnapshot) ==
         floatBytes(rasterOf(map, [point], 32, tick))
 
-  test "scheduled rebuilds and empty selections leave the cache consistent":
-    let map = wallMap()
-    let nav = newBodyNavSystem(map, 2, 331, dangerK = 1)
-    let a = (x: 40, y: 30)
-    nav.initializeDanger([input((8, 8), [a]), input((8, 8), [])], 0)
-    check nav.dangerSourceCacheOrder.len == 1
-    nav.rebuildScheduledDanger(1, [input((8, 8), []), input((8, 8), [a])])
-    check nav.dangerSourceCacheOrder.len == 1
-    check floatBytes(nav.seats[1].dangerSnapshot) ==
-      floatBytes(rasterOf(map, [a], 331))
-    check nav.seats[0].dangerSnapshot.allIt(it == 0'f32)
-
   test "cached sparse boxes preserve border indices across word and row sizes":
     let map = wallMap()
     let points = [(x: 8, y: 8), (x: 472, y: 280), (x: 240, y: 144)]
@@ -212,3 +322,6 @@ suite "shared danger source visibility cache":
       nav.seats[0].rebuildDanger(map, input((8, 8), reversePoints), 2)
       check floatBytes(nav.seats[0].dangerSnapshot) ==
         floatBytes(rasterOf(map, reversePoints, range))
+      nav.seats[0].rebuildDanger(map, input((8, 8), reversePoints), 3)
+      check floatBytes(nav.seats[0].dangerSnapshot) ==
+        floatBytes(rasterOf(map, reversePoints, range))
diff --git a/tools/bench_body_nav_rework.nim b/tools/bench_body_nav_rework.nim
index 49f367d7..9a21c70b 100644
--- a/tools/bench_body_nav_rework.nim
+++ b/tools/bench_body_nav_rework.nim
@@ -265,7 +265,7 @@ proc activationRow(gate: MapGate; gunRange = NavCorpusGunRangePx): JsonNode =
     bytes.mixedGraph + bytes.packedWeightScratch +
     bytes.sharedDangerGeometry + bytes.sharedDangerSourceCache +
     bytes.trace + bytes.allocatorOverhead
-  const PoolSharedCap = 32'i64 * 1024 * 1024
+  const PoolSharedCap = 64'i64 * 1024 * 1024
   let totalNs = mapNs + indexNs + hazardNs + cacheNs + navNs
   let activationRatioLimit = if gate.colossal: 3 else: 2
   let activationTimePass = totalNs <= mapNs * activationRatioLimit
