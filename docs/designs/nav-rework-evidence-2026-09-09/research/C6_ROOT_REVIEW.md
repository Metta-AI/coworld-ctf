# C6 root review and micro screen authorization

PROCEED with an isolated micro screen in nav-source-cache only. Preserve C5 v2 bytes first (already archived); compare frozen C2 against C2 plus the full-word arm. Do not build on provisional C3: it has not earned adoption, and the sparse loop must remain byte-for-byte C2. Root handles native runners and primary integration.

Corrections: no numeric count threshold was preregistered, so call the16–30% observed full-word share a descriptive screen supporting this decision, not a passed preregistered gate. The half-cost scenario is not a hard speedup ceiling;8–15% of replay times70% of a fill would be5.6–10.5% of a fill, not automatically2% of whole-body p95. Keep hypotheses separate from bounds. Do not add full-word scanning counters inside timed production loops.

Implement the exact row-split full-word sketch, no new memory or changed source/floor order. Add a tools-only replay microbench that warms each selected source outside timing and times repeated calls to the actual private replayVisibleCells. Clear/parse/hash outside timing, record full raster hashes after each batch. Use configured map3 and two real-trace maps, ranges331/1300, sampled fixed valid origins and the same source sample sets in both arms. Report time per replay, sample batches and a direct set-bit/reference comparison. No clock inside route choice.

Add a tools-only correctness diagnostic that constructs valid in-grid full words crossing row boundaries (diameter<64 and327), plus sparse and mixed words, comparing every float cell to the existing per-set-bit formula. It is fine to construct private test state in a diagnostic include to exercise a branch actual maps rarely reach; do not claim an impossible map fixture. Keep the final partial word's padding zero. Run existing focused cache tests and full trace hash-chain equality; archive all source/tools/results in C6/. Raw count tools/results currently under peer-proofs must stay durable.

Native preregistration (root runs): five paired micro runs per arm on m8i CPU5, predicting at least10% lower median per-replay time in1300px rows before whole-body work. Also replay all9real traces, require identical chains and no material rebuild regression. If that screen fails, close C6 without a primary source change. Parent/candidate must have no diagnostic counters in timed binaries. Exact ledger must be unchanged. End C6 MICRO READY and wait.
