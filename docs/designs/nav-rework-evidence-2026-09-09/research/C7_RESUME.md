# C7 resume after C8 exactness screen

C8 snapshots and exact kernel-support counts are frozen and root native m8i comparison is running (PID39840). C5 m5a attribution confirms replay is the largest first-fill stage; its traced values are diagnostic. C3 and C6 rejected in local checkpoint98c71887.

PROCEED with the bounded C7 tools-only micro from C7_ROOT_REVIEW.md in your isolated cache tree. Preserve all C8 snapshots/source before work. Use actual ray first-visit order and compare captured lists against production bitmaps/raster. Update the older full-box capacity instruction: use an exact capacity equal to the number of nonzero kernel entries, computed from the actual kernel during initialization, with no observed-source bound, overflow fallback, sorting or source-order change. Drop zero contributions as justified by C8, but benchmark against both frozen C2 and C8 bitmap replay so zero omission and list representation are distinguishable. Reuse C6 v2 evenly-spread64origins; three maps,331/1300px; clear/hash outside timing; root owns native runs. A micro does not change32/64slot cache policy or establish whole-body benefit.

Preregistered advancement threshold remains20% median replay reduction versus C2 on each1300px map and no material331px regression; report against C8 as ablation too. Full trace miss-recording/cache-policy and integrated memory ledgers are separate later gates. No primary source or cap changes. The current integrated total is229,158,347B, not266M;32slots*54173cells*8bytes=13,868,288B at1300px before owner/length metadata. Keep cap sizing as a proposal until exact ledger.

Write frozen snapshots, source hashes, commands/exits, crafted list/index/raster proof and C7_MICRO_READY.md. End C7 MICRO READY and wait.

## Root update while C7 is active

C8 native timing completed27pairs, all totals slower; see C8_ROOT_RESULT.md. Standalone C8 rejected, quality still running. Keep the planned C2/C8 ablation in C7. After C7 MICRO READY artifacts are frozen, please review A7_PROPOSAL.md documents-only and write A7_PEER_REVIEW.md (correctness, alternative, worth testing); do not implement A7. End A7 REVIEW READY and wait.
