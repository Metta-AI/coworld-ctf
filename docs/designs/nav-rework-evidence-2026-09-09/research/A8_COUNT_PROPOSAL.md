# A8 proposal: identical pixel-search input recurrence (counts only)

Observation: A5 map48 profiles show repeated pending-seed resolution and finalPending pixel search; A6/A7 local predicate changes did not solve m5a activation. `pixelPathInBox` reads immutable map geometry and routePoint's fixed coordinate decoding; its mutable scratch implements visited/target generations. Targets are explicit ordered refs. Repeated inputs may recur across pending passes even as the outer graph changes. That is a hypothesis, not evidence of duplicate work.

Research before design: Python's standard `functools.cache` illustrates caching by complete argument identity and warns about retained arguments/results and stateful functions (https://docs.python.org/3/library/functools.html). Nim's existing `std/tables` supplies hash lookup with key equality (https://nim-lang.org/docs/tables.html), so a custom hash map or production dependency is unnecessary. We first need a recurrence count, not a cache implementation.

Root proposed diagnostic in root-owned nav-early-visited at frozenf9dff753: rename privatepixelPathInBox body to raw and add wrapper that ALWAYS executes it, recording exact ordered input key (start,center,exactEdges,targets) per index construction. Table resets at every initPixelSearchScratch (crossing and pocket stages have separate scratch lifetimes), as well as between maps; no pointeridentitykey, hash-only equality, reorderedtargets, or result substitution. Capture complete result (reachedTarget,targetRef,points), count identical-input outcomes and any mismatch, plus BFS dequeues for each call. Separate failed/empty, reached-but-no-path, and successful recurrence; include exactEdges mode. The wrapper may own copies only in diagnosticbuild; source must restore afterward. Target refs decode from immutable map dimensions, but independentlyverify this before calling inputs sufficient.

Start map48 and a fewconfiguredmaps; only consider an implementation proposal if repeated empty-result keys account for at least25% of raw BFSdequeues and at least100repeated empty-result calls onmap48, with zero differing outcomes for an exactkey. This is an operation-count advancement screen; no native timing or adoption claim. Any later cache must audit scratchgeneration rollover, transientboundedmemory and invalidation, and reprove76indices/fullquality.

Peer: after C9 FULL READY, review this bounded countproposal documents-only. Do not implement A8 or alter nativehosts. Root owns counts implementation if agreed. End A8 COUNT REVIEW READY and wait.

Root source audit: routePoint positive refs decode from cell index and fixed gridWidth; negative refs decode a16phase lattice from fixed map dimensions. PixelPath reads canStand/segmentClear from BodyMap, not mutable graph labels. These support complete explicit inputs but do not replace the diagnostic output-equality check.

Pre-run correction afterpeerreview: original1000callminimum was impossible given the already-recorded A6 total457calls/map48. Rootverified A6_SCREEN_REPORT lines58–79 before any A8measurement. Replaced with100repeatedemptycalls plus unchanged25%dequeuefraction; this corrects the proposal before running, not a post-result threshold change.
