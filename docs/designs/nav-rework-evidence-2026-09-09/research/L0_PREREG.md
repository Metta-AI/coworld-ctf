# L0: preserve work when danger inputs are exactly unchanged

Purpose: close the inherited stationary-threat completion failure. This is a
qualification prerequisite, not a declaration that the later >=2x research
target has been reached. M0 (ring131072) is the immediate parent.

Candidate: compare the exact ordered selected source points and count with
those used for the retained raster. Map and kernel are immutable. If equal,
refresh dangerTick and skip raster/packed computation and generation bump.
No hash, approximate comparison, extra retained raster, or new dependency.
Preserve installed-route expiry on every scheduled refresh, including when
inputs are unchanged, to retain the existing bound on zone-price freshness.
Changed sources still rebuild and invalidate before another search pop.

Tests: identical inputs preserve in-flight job, changed inputs still cancel,
source removal and return both change generation, same inputs still expire
installed routes on schedule, original corpus route hash unchanged. Run
latency twice on real Xeon with all failures retained; no cap or threat edits.
M0 and L0 are separately measured. After correctness, use same four-budget
matrices on m5a/c6a/Xeon and separately characterize changing threats.
No claim that unchanged static threats always preserve ordered selection:
self movement can reorder source points, especially near ties or >8 sources.
