# M0 checkpoint documentation audit

Applied audit-documentation to the current session diff before committing.
AGENTS.md now describes real-EC2 tick qualification,3.6/4.5 selection headroom,
separate completion-latency command, native versus canonical quality commands,
and the pool16MiB/total256MiB allocation gates. Each claim was checked against
the handoff, harness argument parser, activationRow and default Dial constant.
Research preregistrations and reports retain the failed m5a and latency rows,
failed canonical invocation and transfer race; they do not claim Phase10 done.
The recorder script change supplies the existing supported runtime compile
flags, with no change to its arguments or recording recipe; bash syntax passes.
Viewer rebuild and node module/source-stamp evaluation pass, GV63 guard passes.

Outstanding broader Phase11 audit: design+HTML twin, BR_PLAYS, demo docs,
final budget/fixtures, full suites and canonical gate. This checkpoint does
not state these are complete. L0 is labeled unimplemented preregistration.
