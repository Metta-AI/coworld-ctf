# Independent next-hypothesis review: exact ray-prefix sharing

D0 measured three m5a1300 pairs: worst body p95/max5.948740/6.034892 ->5.838907/5.953172ms; all18 paired rows improve p95, masks/pops identical. Full m8i quality/retained ledger identical. Still fails. D1 kernel-index ablation remains next small change.

Please investigate a larger exact alternative, documents and standalone counts only, no source edits: build a shared static trie of ray step prefixes for each fixed perimeter/radius, then walk common prefixes once per source. Preserve the exact X/Y/XY side-wall semantics and union of visited cells. Within one source each cell gets one identical kernel addition; source order remains unchanged, so changing visit order within a source may be safe even though changing source order is not. Test that reasoning carefully against addVisibleCell and the close-floor pass. Branches must still stop at the same walls, including diagonal side checks. Count original total steps versus unique prefix nodes for actual radius42/163 (and supported maximum); estimate actual array capacity and activation cost. No unbounded visibility cache, new threads or approximation. Research established prefix-sharing/trie approaches before recommending custom storage, and explain why any proposed local representation fits this fixed tiny alphabet better than a dependency. If sharing is negligible, reject explicitly. Write DANGER_PREFIX_REVIEW.md with proof holes and measurable hypothesis, no speed claims.

Also note CAP32 now passes all76 memory rows, configuredsharedmax31,607,620B. 75non-colossal maps exceed inherited2x totalactivation ratio; colossal passes3x. No activation waiver exists. This candidate must not hide that remaining gate.
