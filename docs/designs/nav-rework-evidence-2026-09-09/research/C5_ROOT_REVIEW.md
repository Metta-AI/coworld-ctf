# C5 root review before native run

The 144 rebuilds equal the total seat count across all six rows (3*16+3*32). That suggests this entire trace's expensive danger work is the first scheduled fill, distributed across danger cadence, not recurring work. TickWarmups=5 may leave first fills inside measured samples. Before calling this a persistent danger floor, map danger.clear events to each of the 126 shell.danger events per row and report initial/warmup/measured tick indices. Preserve the valid first-fill workload; do not change warmups to hide it. Real traces still matter for ongoing production behavior.

Please finish this diagnostic review in C5/ (same isolated source ownership). Add an explicit ProfileTracePath-only danger.packWeights stage around rebuildPackedWeights in publishDangerGeneration; keep equality comparison/generation publication separate. Preserve the first C5 snapshot/smoke as v1, then provide a v2 snapshot and smoke/recipe. Capture each command exit code explicitly: run_c5_profile.sh currently uses || true and discards it. Profiled row times include tracing overhead and are attribution only, not an acceptance comparator; correct the report's wording that proposes using traced timings for a pair. Root's actual unprofiled C2/C3 comparisons remain authoritative.

A6 m8i surprise: counter-free lazy constructors improve3.45–4.12%, direct loses11.1–11.7%; A6_NATIVE_STATUS.md. Full lazy quality/activation run PID35469 occupies m8i; m5a A6 is queued after C3. No A6 source integrated. Root will launch C5 v2 after host idle. End C5 V2 READY and wait.
