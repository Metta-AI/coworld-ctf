# CAP32: qualify the authorized memory limit

Parent W7; change only the harness non-colossal shared cap from 16 to 32 MiB. Keep 256 MiB total, all capacity/context accounting, quality, activation and timing requirements unchanged. Run fresh native m8i quality/activation and the configured eleven-map screen with the exact W7 remote source snapshot and revised harness; retain input hashes and full source diff. Expected shared maximum 31,607,620 bytes. This is a memory-policy qualification, not a speed experiment or final production acceptance. Prior failures remain in their original artifacts.
