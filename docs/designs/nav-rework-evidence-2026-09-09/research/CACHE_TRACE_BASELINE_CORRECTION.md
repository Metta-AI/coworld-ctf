# Trace workload correction and delegated scope

Current main includes protocol-adaptive baseline support (bccf812c). players/baseline/baseline.nim detects PlayContext and calls runS2Session; s2play.nim loads reference plays from BASELINE_PLAYBOOK. policies/starters/common/build_playbook.sh builds all reference .nims recipes. There are more reference plays than hello_play/edge_ride (loot, crossfire, target_law, supply_run, jackal, bodyguard, etc.). The plan's claim that edge_ride is the most realistic available local policy is contradicted by current source. Revise the gating workload to real baseline S2 clients if its smoke succeeds; edge_ride remains a useful contrast, not the best available policy.

Root creates owned isolated worktree /Users/jamesboggs/coding/coworlds/coworld-ctf-worktrees/nav-source-trace, branch james/nav-source-trace, at current source before instrumentation. Peer is authorized to own temporary trace instrumentation and the bounded runner there, including local builds/smoke and local episode capture, but no remote timing, publishing or edits to root production source. Use the pinned project toolchain, Wasmtime/WASI SDK from the root research worktree where needed, and an explicit local BASELINE_PLAYBOOK. Preserve patches and raw outputs in the research evidence folder inside the trace worktree; root will copy finished artifacts after review. Do not commit diagnostic source into the production branch.

Instrument actual scheduled changed-source ray calls and preserve life/map/order details. Every trace line should carry its system ID, rather than relying on whichever header was last printed. Do not promise binary identity without the define: source line metadata/stamps can change even when runtime logic is excluded. Verify unchanged danger fingerprints with the define and preserve the exact patch; this is a count-only diagnostic, never timing qualification.

Please first revise CACHE_TRACE_PLAN.md with the real baseline, resolve 16-slot/token config shape and finite server exit by code/one smoke, then perform the bounded count capture if it is sound. Do not substitute 32 slots with 16 inactive seats for a true 16-seat workload without reporting the distinction. Root independently handles activation and C1 results; no remote host work from the peer.
