# A0: compute each undirected fine legality edge once

Parent067a4af2 (same source as782af867). Compute four directions and set the corresponding reverse bits. The restricted4px axis/diagonal segment predicate is symmetric; proof and91,240,212 checked directed edges are in ACTIVATION_REVIEW.md. Do not change bridge construction, query behavior, memory layout or thresholds.

Run focused tests before native measurement. On the fresh m8i worktree, build parent/candidate binaries, compare every retained graph array by SHA256 on64 frozen maps,11 configured maps and colossal, then run three interleaved activation pairs and one full candidate3072 quality run. Preserve all raw per-map times and source hashes. The new graph fingerprint tool is common to both arms and hashes fixed-width arrays outside timing. Report activation ratios explicitly: the existing activation pass field checks memory only. No speed claim before measurement.
