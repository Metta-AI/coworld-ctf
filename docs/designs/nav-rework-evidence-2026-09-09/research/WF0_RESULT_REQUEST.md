# WF0 result and next decision

The m5a screen is complete in WF0-screen-m5a/result.json. All 14 raster comparisons pass. At 331 px the candidate loses on six maps (1.08–1.27x parent) and wins only colossal (0.928x). At 1300 px it loses on all seven (1.99–3.57x). This overrides the encouraging Mac pattern for our bottleneck host.

Please independently review these results, then propose one bounded next screen with an explicit cost argument. Is per-shell active-range narrowing actually likely to beat ordinary rays, including index/list overhead and the same-shell removal rule? Consider zero-kernel tail pruning as a separate minimal exact candidate. Do not implement production code or mutate the remote worktree; root owns activation work. Write WF1_PROPOSAL.md, including a rejection/stopping rule if wavefront geometry has the wrong work shape. We should not persist with a slow direction solely because its proof is elegant.
