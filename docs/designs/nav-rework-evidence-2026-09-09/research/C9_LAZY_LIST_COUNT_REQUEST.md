# C9 conditional count screen: defer auxiliary-list materialization

C7full native traces/regimes/quality are being launched on both owned hosts. No result yet; full miss recording is26–31%slower in local diagnostics. PROCEED with documents/counts only, preserving frozen C7full source. No new implementation or native job.

Established background: cache admission policies avoid spending capacity on entries without sufficient reuse; Caffeine describes WindowTinyLFU and delayed policy decisions (https://github.com/ben-manes/caffeine/wiki/Design). This does not prove a speedup here or justify importing a Java cache into Nim. Our proposed adaptation is simpler: retain C2bitmap recording on misses, and materialize the auxiliary list only on a cache hit, replaying/building in one traversal. Subsequent hits use the list. Keys/LRU/sourceorder unchanged,bitmap and list remain preallocated64slots. The extra bitmap payload is about0.86MB at1300px and appears within the proposed64MiB cap, but must be ledgered. A separate length sentinel distinguishes unmaterialized from an emptylist.

Before implementation, independently replay each of the9traces with64-slot sequential per-source LRU (update on each source, not pre-block classifier). For each residency count total sources, misses, firsthits, laterhits and histograms of0/1/2/3+hits. Separate each map/roster/episode; preserve exactkeys andsourceorder. Screen materialization at firsthit and at secondhit, counting how many conversions and later list replays each policy earns. Do not fit thresholds to hide negative traces.

Use C7native list/bitmap ratios only as an explicit cost model: saved later-hit bitmap time versus one-time materialization overhead still unknown. Report the maximum extra conversion cost (in units of bitmap replay) each trace could afford before breaking even. Counts alone cannot establish speed or authorize C9source. Identify alternative if most entries never earn anotherhit. Write C9_COUNT_REVIEW.md, executable countscript andJSON; end C9 COUNT READY and wait.
