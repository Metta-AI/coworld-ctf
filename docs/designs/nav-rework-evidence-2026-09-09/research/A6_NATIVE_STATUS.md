# A6 native result: lazy arm earns a follow-up, direct rejected

Three interleaved sets of five constructors, m8i CPU5, no pixelStandabilityCounters in timed binaries. Parent index means 253.979, 253.579 and 252.567 ms. Direct: 282.149, 282.171, 282.208 ms (11.1–11.7% slower). Lazy: 243.525, 244.827, 243.694 ms (3.45–4.12% faster). The prediction of no useful lazy improvement is falsified on this host; preserve that surprise instead of treating the Mac result as a gate. Raw A6-m8i/ traces and A6-native-summary.json retain every stage/repeat.

Root independently compared the peer's local outputs: all 76 selected route-index array hashes and counts agree, as do crafted search results and the 173,424-search map48 chain. The identity diagnostic does not hash every struct array: pockets/segments/arcs/sides are counts only. Do not inflate that coverage into complete graph-byte identity.

Direct is rejected. Lazy needs an unprofiled full quality/activation/memory run, focused tests with the lazy define (the initial focused runs used eager), and m5a native paired validation before any integration. A4 is present in both A6 arms; no C2/V1/H1 source effects are inferred. The additional 16,900-byte lazy scratch is transient and must stay accounted. No memory cap or timing threshold change, and no final B selection.

Follow-up m8i: all 3,072 quality cases pass with the pre-H1 hash 5a1340213fe3046dc119d6f8d5d59d59cbfd379f885b95968f105d3edcbca500. All 76 activation-time and memory rows pass, and all retained ledgers equal A4. Lazy focused index tests pass 7/7. Configured tick timing still fails on 8/11 maps, worst p95/max 6.059062/6.270696 ms; this isolated activation experiment omits C2 and has no runtime speed claim. m5a paired screen is queued behind the complete C3 runner.

Native m5a screen: lazy improves index means2.47–2.84% (parent837.985/840.126/839.258ms, lazy814.843/819.369/815.459). Direct loses17.2–18.1%. A full m5a unprofiled quality/activation/configured follow-up is now running in A6-qualify after C5 completed. No integration yet.

Completed m5a full qualification: 62/76 activation-time and76/76 memory rows pass; worst ratio2.2671168807,14failed maps. All retained ledgers equal m8i. Full3072quality reproduces the pre-H1 hash, focused index tests pass7/7. Configured whole-body0/11maps pass, worst p95/max 15.738642/15.873816ms; this isolated arm omits C2 and is not the integrated retained runtime. Both owned hosts restored cleanly after their runners. A6 is still not integrated.
