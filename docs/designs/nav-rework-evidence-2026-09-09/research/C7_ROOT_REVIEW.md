# C7 root review: correct the premises, then a bounded list micro screen

The rejection relies on two premises that are not current or applicable.

1. The colossal figure266,538,427 B is isolated C2 before V1. Current integrated C2+V1+A4+H1 is229,158,347 B (C2-integrated-memory-summary.json), leaving39,277,109 B under256MiB. A32-slot full-box1300px list is27,373,824 B, replacing the bitmap cache; allowing the larger1300px kernel still appears feasible. Recompute exact ledgers, but do not call the obsolete1.9MB margin a blocker.
2. Roaring's4096/65536 threshold is a storage/container decision for compressed set operations, not a benchmark of weighted scatter-adds with precomputed float values. The cited papers' intersection and compression results do not establish a universal iteration crossover for our kernel. Root verified the primary arXiv abstracts; retain the papers as relevant background, not proof that this list must lose. Row-major versus ray-order and extra bytes are real tradeoffs to measure. Also13.4+44.8=58.2KB versus89.7KB is1.54x source reads, not2x; common raster reads/writes must be counted consistently.

After C6 V2 NATIVE READY, PROCEED with a tools-only C7 micro screen in the isolated cache tree. No production source or cap change. Use current frozen C2 and C6 replay references plus a preallocated (int32 gridIndex,float32 weight) list built in ACTUAL first-visit ray order. Do not claim that iterating the row-major bitmap recovers ray order. A diagnostic copy of the production first-visit ray walk may record the order during an untimed warm rebuild; compare the captured list's one-add-per-cell set and float raster against the production bitmap before timing. Keep exact source order, one add per cell and floors outside this replay-only micro. Full-box capacity only; no observed-max cap, no sorting.

Use the same v2 whole-map-spread64origin sample sets and JSON coordinates as C6; list capacity32 is an eventual production proposal, not a reason to bias micro samples. Full lists are diagnostic scratch. Three maps,331/1300px, repeated replay batches with clear and hash outside timing. Root will run native. Preregister >=20% lower median replay time versus C2 on1300px rows and no material331px regression before considering an integrated32-slot design. Miss recording and reduced-hit32-slot policy need their own later paired full-trace test; a fast hit loop alone does not authorize adoption.

Own only C7 diagnostic tools and C7/ evidence plus corrected review. Preserve C6 snapshots/tools before changes. End C7 MICRO READY and wait. Root owns native runners and cap decisions within user authorization.
