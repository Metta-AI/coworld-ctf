# C7 micro and A7 evidence checkpoint audit

No primary production source, cap, budget, config or gameplay change remains in this checkpoint. C7 micro is a positive isolated replay result on both hosts, not a complete source-cache or whole-body result. The64-slot memory substitution is an estimate based on measured integrated ledgers; the constructor ledger and authorized64MiB cap change remain future conditional work. C7full implementation is still peer-owned and excluded from this checkpoint. A7 did not reach its5%both-host index-construction threshold and is not integrated.

Updated scoreboard and native reports, retained original failed/revised micro protocols and raw paired samples. Audited path/index exactness claims against76selected-index fields,173424searches and correctedv2exactEdges diagnostics; the original crafted pinch did not distinguish modes, while12real-map pairs do and remain identical across all four arms. Restored every native and local experimental source after runs. The new A7 checkout required canonical nimby sync to generate its config; the initial missing-dependency failure is preserved.

Canonical user-facing docs still describe the retainedC2implementation and32MiBshared/256MiBtotal caps correctly. Final budget/version/fixtures/viewer/full-suite/containment/Docker/fleet qualification remains open and is not advanced by this checkpoint.
