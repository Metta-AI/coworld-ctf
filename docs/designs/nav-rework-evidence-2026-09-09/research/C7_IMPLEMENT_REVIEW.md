# C7 full64-slot list candidate: isolated implementation gate

Root accepts the64-slot recommendation and native micro passed (C7_NATIVE_MICRO_REPORT.md). PROCEED in nav-source-cache only, preserving all C2/C8/C7v2 snapshots first. Root owns A7 elsewhere and both native hosts. No primary source/cap changes.

Implement the ordinary production miss/hit paths with one system-owned preallocated contiguous seq of (gridIndex:int32,weight:float32),64slots, unchanged key/LRU policy. Capacity per slot is counted from actual nonzero kernel entries during initDangerGeometry/cache creation, no observed-max bound and no overflow fallback. Store length per slot, reset on miss replacement; add exactly once per nonzero first-visited cell in actual ray order after the existing visit stamp. Replay stored entries for that slot. Keep source order, exact-pixel close floor, ray traversal/perimeter, visit-generation semantics and all publishing behavior. No C3/C6, no experimental booldefines in the materialized candidate. Existing ref owners and one allocation should replace the bitmap allocation directly; inspect slot padding before adding unnecessary metadata.

Update the capacity ledger to actual sizeof/seq.capacity with existing allocation allowances; do not change pool cap constants yet. Native paired trace candidate is isolatedf9+C2-to-list; oldpreV1colossal cap failure is not an integrated memory verdict. Root will apply the minimal candidate atop integratedV1/H1/A4 to verify actual76memory rows and exercise64MiB authorized cap if earned.

Freeze parent/candidate snapshots and patch. Run focused cache/nav tests, nine actual source trace chains (input/hit/miss/rasters exact), changing/repeated-source diagnostics, and a capacity ledger across ranges331/1050/1300/1600. Copy primary multi-range border and byte-stamp rollover tests only if applicable to isolateduint32parent; preserve primary tests unchanged. Important: existing cachetests may assume bitmap bytes; update those assertions to the new measured representation without weakening raster/LRU correctness. New tests should target real list overflow/index/eviction risks, not restate implementation.

Root next: three interleaved native trace/regime pairs on both hosts, unprofiled full quality on oldhash, then integrated76memory/wholebody. Success requires actual trace total benefit, no material miss regression and exact outputs; a micro alone is insufficient. Report all negative rows. End C7 FULL READY and wait.
