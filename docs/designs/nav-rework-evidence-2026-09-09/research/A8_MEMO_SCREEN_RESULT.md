# A8 failed-search memo: native screen negative

The candidate fails the preregistered requirement of at least 5 percent median map48 index-construction improvement on both native hosts. No adoption and no threshold change. The m5a result is close to the threshold; this three-pair screen does not establish a precise underlying effect, but its registered decision rule is unambiguous.

| host | median of paired ratios | reduction | decision |
|---|---:|---:|---|
| m8i-flex | 0.938440693 | 6.15593% | pass |
| m5a | 0.950314155 | 4.96858% | fail |

Three interleaved process pairs per host, five constructor samples per process, CPU5, canonical Nim2.2.6 release/useMalloc/noSignalHandler flags. Each pair compares the median of its five samples; the screen uses the median pair ratio. Parent is the retained A4 index, not the pre-A4 diagnostic source. Frozen sources, runner, raw samples and native metadata are under A8/memo/. native-screen-summary.json contains the calculation. No timing on the Mac is used for the screen.

All 19 retained sequence fields and non-transient statistics match on all 76 maps. Map48 counts exactly match the model: 189 hits, 356,784 saved dequeues from 805,625 baseline. Five focused tests cover reuse, rollover, key distinctions, full capacity, oversized lists, invalid starts and success. Actual Mac temporary memo size is37,896bytes; retained arrays and retained memory are unchanged. Generated C uses an output parameter to construct scratch in place and direct field access during lookup. This does not prove improvement on maps without repeats or solve the absolute activation/tick gates.

No full native quality/activation campaign is warranted by this failed screen. Primary sources and memory caps remain unchanged. Native runners restore their original source on exit. Root is restoring the isolated local source after preserving its snapshot. Pending exact-path check and peer source review will be recorded as supporting correctness evidence, not a reason to reopen the timing decision.

Peer: finish the source review already requested, then review this result and native-screen-summary.json. Confirm the negative decision; do not propose a resample to cross the same threshold or start implementation. Update A8_MEMO_SOURCE_REVIEW.md with the screen outcome; end A8 MEMO REVIEW FINAL READY and wait.

Exact-path follow-up: the complete A7 v2 crafted-case outputs and real-map48 path-chain output match the frozen eager reference after removing only the arm label. The initial tool build failed because its legacy diagnostic arm-label constant was absent; replacing the output label with a literal corrected the harness. Both logs are retained. No source algorithm correction was needed.

Memory interpretation: the existing transientPeakBytes statistic takes the maximum recorded allocation stage, not the sum of all simultaneously live scratch structures. It stayed unchanged on all76maps despite the additional37,896bytes of stack scratch. The added bytes are real; this statistic must not be presented as a complete peak-memory measurement. The candidate is rejected, so no production memory accounting was changed. First-N admission is fitted to observed early failures on map48; the other75maps had no recurrence. Late repeats after capacity fills would see only scan cost.
