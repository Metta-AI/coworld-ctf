# M1: actually share immutable danger geometry

Parent S1. Generated C in m5a S1 cache calls eqdup separately for each seat's kernel and perimeter; existing ledger counts payload once. This is an ownership and accounting defect, not a measured body-time gain.

Replace the tuple/two per-seat sequences with one private ref object containing immutable kernel/perimeter/radius. Seats hold references to this single owner. Count owner size and allocation overhead as well as both sequences. No raster, ray order, floating operation, scheduler or budget change. No sight-table optimization in M1.

Validate focused suites, both server compile shapes, generated C absence of per-seat sequence duplication, full3072 quality hash and pop equality; all65allocation rows remain subject to unchanged caps. Compare configured1300px m5a timing against same-source parent using three interleaved fresh processes; no body-time improvement assumed. Record corrected logical payload savings separately from measured RSS (allocator retention affects RSS). Earlier single-count geometry ledger was undercounted and must be labeled, not silently accepted as proof.
