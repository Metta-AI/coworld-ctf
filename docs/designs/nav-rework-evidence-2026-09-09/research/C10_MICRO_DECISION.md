# C10 micro decision, before native timings

Root accepts the three-arm ablation: retained C2, grouped scalar, grouped SIMD. Root owns all implementation and native jobs; peer remains read-only reviewer. Hoist raster/kernel pointers and lengths identically in both grouped arms; do not copy Nim sequences, whose ownership/copy cost would confound the result. The four-cell safety check must precede all pointer access. Keep the scalar boundary fallback checked.

One correction to the proposed selection rule: do not reject SIMD merely because scalar exceeds20%. That would discard a potentially much larger gain needed for the user goal. Prefer scalar if it clears the20%/1%control screen and SIMD improves it by under5%; retain SIMD for the next screen only if it clears the20%/1%control screen and improves scalar by at least5%on each1300map on bothhosts. If neither qualifies, close the unit. These rules are fixed before seeing timings. Five interleaved process triples, five batches/four repeats/64even-spread origins per process, CPU5. Full-source/changing-trace screen later uses five pairs on bothhosts, same1%limits; no early whole-body claim.

Root's existing ARM correctness passes five C6-crafted cases and128border/mask cases at104px, including negative-zero preservation. Extend the border/mask sweep to1300px and compare every real-map float cell in the same native micro before timing. Both scalar and SIMD must pass. The current prototype note predates these additions; raw files retain them. Both native hosts are idle; source restoration was verified afterA8.

Caution on NEXT_THROUGHPUT_UNIT_PLAN.md: summed per-stage p95 values and traced stage means are useful scale estimates, not a mathematically exact ceiling or proof that no single other unit can close a gate. Planning not tracking map-to-map variation does not prove optimizing it cannot contribute to acceptance. Keep these as approximate prioritization, and do not assert hardware streaming floors without measurements. No new scope or safety gate follows from those estimates.

Peer task: after root freezes the revised helper/tests, verify this selection-rule correction and the pointer safety/three-arm fairness. Root may compile and run correctness now; timed micro waits for the bounded source review. End C10 MICRO REVIEW READY and wait. Do not expand to other units while reviewing this one.

Before launch, statistic fixed: within each host/map/range, pair arms by repeat index; take each process median across its five batch timings; compute scalar/parent, SIMD/parent and SIMD/scalar; select using the median of five paired ratios. Each1300map ratio to parent must be<=0.80, each331control<=1.01, and SIMD incremental ratio<=0.95 on every1300map/bothhosts. C10/summarize_native.py implements this and asserts equal accumulated batch raster hashes across allarms; its pre-launch SHA256 is 21ddd683ce526830c024bcf8779bba6005bf1acfa6ef569b92ba43e7d05f1dd7. Peer C10_MICRO_SOURCE_REVIEW.md finds no blocking defect. Root accepts review and authorizes the frozen native micro only.
