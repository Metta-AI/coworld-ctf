# C2 documentation audit

Reviewed the cache implementation, public ledger, benchmark schema, focused tests, isolated native results, and combined quality output. C2_STATUS.md supersedes the running-job status. C2_INTEGRATION.md records the byte-stamp merge, exact-goal hash, mutation evidence, and the 16-byte owner-allocation correction. SCOREBOARD.md links the result and preserves the configured timing failure and changing-source regression.

The new shared_danger_source_cache ledger field reports one system-shared 64-entry LRU with a contiguous bitmap allocation. It is keyed by 8-pixel origin cell and bounded by the immutable range/sight geometry of its owning system; per-source floors still run in original source order after bitmap replay. The cache changes execution cost, not danger rasters or route semantics. It neither changes the 32 MiB non-colossal shared allowance nor the 256 MiB total allowance. No gameplay/config field changes, so ENV_VARIATION needs no new row.

Developer diagnostics: tools/bench_body_danger_cache.nim compares changing/repeated source regimes; tools/bench_body_danger_trace.nim replays archived NAVSRC selected-source sequences. Build with the project Nim toolchain and normal release/useMalloc/noSignalHandler flags. The trace include also needs --path:src/shell. Optional -d:dangerSourceCacheCounters adds diagnostic counters. Neither tool is a whole-body acceptance gate; source parsing, output and trace raster hashing are outside timed rebuilds. run_c2_mechanisms.sh preserves the exact native recipe and immutable inputs.

The authoritative design HTML and final qualification report still need the complete campaign refresh in Phase 11. This local research checkpoint does not claim that phase complete, does not select B, and does not erase the 18 isolated shard-4 replay/golden failures. Final nine-fixture recording, fleet and canonical Docker gates remain required.

Follow-up canonical guidance audit: AGENTS.md still stated the superseded 16 MiB cap and treated the 2,000-tick observation ceiling as a latency requirement. Corrected both, documented the capacity-ledger limitation and configured workload command. The design HTML now points to current implementation research instead of claiming implementation has not started, and reports the fresh C2 shared bound. MEMORY_CAP_RULING.md records the current measured margin. The full historical design-body rewrite remains Phase 11 work.
