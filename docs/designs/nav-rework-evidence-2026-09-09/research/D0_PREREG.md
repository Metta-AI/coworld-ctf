# D0a: incremental danger-ray decision

Parent W7 (addaad7a); shared-cap32 policy is common to both timing arms. Replace the per-step integer decision recomputation with its exact incremental recurrence. Keep cell visits, side-wall checks, float additions, perimeter order, all compiler checks and memory layout unchanged. No kernel-index changes, geometry hoisting or precomputed move table in this unit.

The existing supercover integer ray algorithm already supplies the recurrence: initial ny-nx, X adds 2*ny, Y subtracts 2*nx, diagonal adds their difference. See independent D0_REVIEW.md for the algebra; no dependency is needed for these few integer operations. Focused danger/navigation suites must pass before native timing. Run three interleaved m5a CPU5 parent/candidate fresh processes at B1024 and range1300, preserving full row masks and pop arrays. Full frozen3072 quality on m8i follows CAP32 without concurrent measurements. Require identical corpus and danger hashes, masks, pops and retained bytes. Report whole-body and inclusive danger p95/max separately from weight refresh; do not subtract percentiles. No speedup assumed; preserve negative results. Final fleet/budget selection is separate.
