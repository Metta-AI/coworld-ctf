# C3/C6 negative-experiment documentation audit

Reviewed primary source diff, retained test, C3 final review and all three C3 summary files, C6 native report and two native summaries, scoreboard and canonical retained implementation guidance. Neither experiment is retained in production. C3 primary source and viewer were restored byte-for-byte from HEAD; C6 never changed primary source. The sole retained code change is a regression test for existing cache behavior across ranges and map borders.

Updated C3_STATUS.md to replace running/provisional claims with completed results and rejection; scoreboard and ledger record both decisions. Canonical gameplay, budget, memory caps, build workflow and public APIs are unchanged, so no new GameVersion, fixture recording or canonical user-documentation edit is due for this checkpoint. Native experiment traces, counters and microbenchmarks are not whole-body acceptance. Existing final campaign qualification and replay failures remain open.
