# M2: retained sequence capacities

Capacity counters replace logical sequence lengths; no allocation compaction. The regression reserving64extra bridge nodes without changing contents passes and increases the ledger by256bytes.

All65original activation rows pass the current measured capacity gate: pool shared maximum16059225bytes; colossal total265221452bytes. All11configured giant maps still fail16MiB; maximum shared31340947bytes. RawM2-m8i retains both full outputs and source snapshots.

This counts sequence payload capacity plus the existing allocation-header allowance; it is not a physical RSS measurement. An additional ownership-boundary review of hazard/safe-cache allocations is pending, so this does not complete the overall memory audit. Configured timing in this combined-source screen is diagnostic only, not a paired M2 performance claim.
