# M3: include installed hazard and safe-cache ownership

Accounting/workload correction only. Add the shared overlay/cache payload capacities and allocation allowances to the navigation ledger through the safety context that already holds them. The activation harness must build the armed overlay and refresh its cache before the nav constructor releases hierarchy following payload, install the context, then read retainedNavigationBytes freshly. Keep separate overlay/cache activation durations and include them in total activation.

Run all65original activation rows and11configured maps with unchanged caps. Preserve earlier bare-nav evidence. Query timings with an installed context are a different workload, not an optimization comparison. Keep the3072-case quality corpus untouched. Test that installing and populating the context increases the fresh ledger by its owned bytes and that the total includes both categories.
