# C8 native protocol

Frozen C2 plus inert C5 markers versus the zero-contribution omission only. Native m8i CPU 5, no concurrent timed work. Three paired runs of all nine recorded traces and changing/repeated-source diagnostics; arm order reversed in repeat 2. No counters or Fluffy instrumentation in timed builds. Input sequence and raster chains must match. Full 3072-case candidate quality must reproduce the old pre-H1 hash. Timing is mechanism evidence, not whole-body acceptance.

Advance to the preregistered m5a short/map-3 whole-body pairs only if the actual traces improve without material regression. Null closes standalone C8. Preserve every raw result and report hit/miss tradeoffs from the isolated count screen separately.
