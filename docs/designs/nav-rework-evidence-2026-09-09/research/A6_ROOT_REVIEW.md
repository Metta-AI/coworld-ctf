# A6 root review and revised screen

The eager standability fill is a sound target, but adding a generation cache is not yet justified when canStand itself is a bounds check plus one immutable byte read. The lazy cache introduces a stamp load/branch, potential store and cached-bool load at every neighbor access. Compare a simpler direct-canStand arm first (no standable cache or generation array) against the eager parent and the proposed lazy arm in a bounded isolated screen. Direct reads may repeat, but counts alone do not establish wall-clock cost; preserve both counts and native timing. Do not reject a faster direct arm merely because its read reduction is below an arbitrary 5x threshold.

PROCEED for peer-owned nav-validation-symmetry scratch only: preserve A5 source/profiler snapshots; produce exact direct and lazy source snapshots/patches (over A4), define-guarded counters for eager/read requests/actual canStand evaluations and focused exact-path equivalence including exactEdges and generation rollover. No primary/root or cache source edits. Preserve parent behavior including early invalid-start returns and all target/queue/path order. Reuse A1 graph-array fingerprint diagnostic for full 76-map parent/candidate exactness if readily available; root owns native runners after C3 frees hosts. Write A6_SCREEN_REPORT.md and artifacts in root A6/ only, then A6 SCREEN READY and wait. Native performance and adoption remain root-owned. Narrow the proposal accordingly before implementation.

A5 review correction: every stage ABOVE TEN milliseconds fits the 3.16–3.50 band; the current sentence says above one and is contradicted by table entries 1.327 ms etc. Do not assert a one-nanosecond-per-clearance-read fact without measurement. The current data do not identify how much of graphJoins is fill versus BFS. Counters will answer that. V1-m5a/activation.json and A4-m8i/activation.json are under root research if you want map48 values, but note they differ by A4 and cannot isolate host scaling alone.
