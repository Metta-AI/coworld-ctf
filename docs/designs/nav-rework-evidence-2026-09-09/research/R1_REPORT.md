# R1: immutable cell-center sight table

Three interleaved parent/candidate pairs on native m5a CPU5, B1,024, 1,300 px range, Nim2.2.6 release/useMalloc/noSignalHandler. Every recorded mask and pop array matches. Parent worst whole-body p95/max10.367423/10.601260ms; candidate7.984246/8.777049ms. Danger p95 maximum across rows/repeats falls8.101009->3.609313ms. These maxima are not additive across stages. Timing gate still fails.

Full frozen quality on native m8i passes 3072 cases with zero missing/illegal routes and 37637596 pops. Route hash `5a1340213fe3046dc119d6f8d5d59d59cbfd379f885b95968f105d3edcbca500` is unchanged; the per-case danger hash assertions pass. Focused navigation and body-seat suites pass locally.

Full M3 activation accounting passes all65 original maps: shared pool maximum 16193526 bytes; colossal total 266478979 bytes. The11 configured maps still fail shared memory, maximum 31607620 bytes. Extra table capacity and owner fields are counted once in sharedDangerGeometry. Compare raw memory rows with M3 to isolate the added table; configured scope remains a checked-in-manifest screen, not authenticated live configuration.

Retain for combined qualification, pending the registered331px follow-up and full shipping checks. No 2x throughput claim or qualification sentinel. R1-m5a and R1-m8i contain raw rows, source patches and build logs. The remote m8i source differs from the local final source only by wrapping one long line after upload.

The registered331px follow-up completed in three fresh-process pairs on the same m5a binaries (hashes retained). All masks/pops match. Parent worst p95/max 5.693231/5.729582ms; candidate 4.572689/4.644561ms. Danger p95 maximum 3.397756->2.244583ms. This finishes the registered range comparison; final shipping checks remain.

Subsequent user ruling: MEMORY_CAP_RULING.md authorizes higher non-colossal caps. These raw measurements retain their original16 MiB verdicts; current measured maxima fit the selected32 MiB shared allowance.
