# C5: attribute the configured danger floor before another runtime optimization

C3 native m8i recorded traces preserve exact rasters but totals are flat (0.9939–1.0182x); 23/27 p95 pairs regress, up to8.06%. Repeated-source microbench improves5–13%. m5a full configured pairs are still running, so no C3 adoption. C4 complete-raster cache count screen failed: only2–7% real exact ordered-source reuse, no implementation. Root is closing A6 with a small m8i native three-arm profile, pixelStandabilityCounters disabled in timed binaries, runnerPID34515. Do not edit its frozen snapshots.

PROCEED with profiling only in the peer-owned nav-source-cache tree (frozen C2 over f9dff753). Preserve original C2 body_nav and harness bytes first. Do not touch primary source or remote hosts. Add ProfileTracePath-guarded Fluffy markers around whole danger stages: raster clear, cache-hit bitmap replay, cache miss ray walk (one marker per source, not per ray/cell), exact close floor, final scale/max scan. Existing weight-pack marker remains distinct. Enable start/finish trace around --configured-tick in the diagnostic harness, and add optional cache hit/miss counters per completed tick row, after timing. Snapshot complete source+bench and exact diff in root C5/. Root will run actual configured map index3 (br-gen-5120) at1300px, all six rows on native m8i, then m5a when available. No new timings assumed from nested percentiles.

Run a local compile/smoke for valid trace structure only; root Mac is not acceptance. Preserve byte behavior with focused cache tests; no algorithms or caps changed. Produce run recipe and C5_PROFILE_READY.md, end C5 PROFILE READY and wait.

Also correct A6 report wording: the identity tool hashes selected route-index arrays and only counts pockets/segments/arcs/sides. It is strong evidence but not complete byte identity of every graph array. Preserve that limit rather than claiming full arrays. A6 native results will be collected by root; no source adoption yet.
