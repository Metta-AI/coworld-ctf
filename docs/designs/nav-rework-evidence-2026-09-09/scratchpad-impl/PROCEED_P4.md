PROCEED P4. Claude verified the fixup: containment at fixed HEAD max_body_us=5803 (trap wave; baseline trap 5705), ordinary waves 4.4-4.8 ms, body_pass=true; query 8/8, index 7/7, nav 20/20, seat 23/23, shard_2 was the only failing shard and is now the containment test only. Phase 3 accepted at eaa2046a.

Standing rule from this fixup: from now on every phase report must include the isolated containment run (`tests/test_shell_containment.nim` with -d:useMalloc) with per-wave max_body_us next to a same-session origin/main baseline run, not only tests/tests.nim. Activation-time work belongs in Phase 9's activation rows, never in tick work.

Phase 4 reminders: consume `ZoneArrivalField.damage` via a read-only snapshot seam (never `arrival`); dark overlay when not `season2Shell and zoneDamageByPaint and zonePhases.len > 0`; elapsed zone tick passed into the step (no gameStartTick snapshot); BodyHazardOverlay immutable, BodySafeCache separate and re-keyed on generation/backward time; shared query-local memo keyed by directed arc with stamp; hazard advanced cell by cell at 33 Q4/tick with ceil; the adversarial two-prefix test decides the earliest-arrival alternative label. End with the literal line PHASE 4 DONE.
