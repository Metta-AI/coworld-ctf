PROCEED P5. Claude verified: hazard 11/11 (with the layer-boundary guard), first_light_server 15/15, query 10/10, both server compile shapes; ctf imports nothing from shell. Phase 4 accepted at eb3515e1.

Phase 5 reminders: hints are integers only (ticks_until_paint_here -1 for never, ticks_to_safety, safe_dist_px, retreat octant/brads); dry own cell -> 0 and no direction; fixed-cap 512-pop legal BFS when the room has a dry cell (row-major tie-break); else walked chain distance + shared safeDist across the room's portal sides; every returned source rechecked against the dry predicate; JSON `nav` key and binary section 14 appended (never inserted), byte-identical omission when dark; SdkNav behind a present flag; `drop` and all existing key/section order preserved; JSON/binary selected-row equivalence stays green. The `zone_safe_ground` resolution itself lands in Phase 6 with the Intent contract; Phase 5 only exposes the resolver. Include the isolated containment pair in the report. End with the literal line PHASE 5 DONE.
