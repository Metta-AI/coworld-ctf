PROCEED P6. Claude verified: hazard 17/17, view 20/20, binary_view 9/9, play_sdk 2/2, nav 20/20, seat 23/23, first_light_server 15/15, manifest_schema 11/11, both server shapes, containment 4,708 us pass. Phase 5 accepted at fc989898. The layout-move commit is accepted as code organisation (new procs grouped away from the legacy hot path); do not repeat timing-driven reshuffles without saying so in the report.

Phase 6 reminders: `Intent.targetClass` neutral-empty, only "zone_safe_ground", only on navigate_to, literal point still required and validated; canonical key order with `target_class` after `suppress_fire_freeze` and before `v`, `drop` untouched, neutral omitted so every existing emission stays byte-identical; the reserved uint32 in the 200-byte binary standing-intent record carries class id 0/1 (stride unchanged), JSON/binary standing views agree; emit_validator rejects unknown classes as unknown references and refuses a class on hold; resolve the class BEFORE the arrived test in actFromBelief; identity = (class, resolved source cell), never bucket/epoch/provenance; accepted anchor does not move on sub-threshold updates (test cumulative one-cell drift, the 12-tick moving cadence, profile change, epoch/provenance no-churn); setStandingIntent stops cancelling/pinning (the pin and slot deletions themselves wait for Phase 10, so for now just stop the cancel and record the anchor in the coordinator). ShellAbiVersion stays 1. Include the isolated containment pair. End with the literal line PHASE 6 DONE.
