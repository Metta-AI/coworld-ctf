PROCEED P10 — James ruled on 2026-09-09:
1. Contract change APPROVED: production navigation is a pop-budgeted resumable weighted search over the precomputed legal graph (R3.3 mixed graph: 4 px nodes in danger>0 dilate-1 cells and the static wall band, 8 px anchors elsewhere with exact contracted cold chains), exact width-1 Dial queue, ONE shared workspace, stable shortest-job-first admission, per-tick pop budget, production steering for seats waiting on a route. "No full-board search on a play tick" is replaced by these invariants; write them into the design doc, BR_PLAYS/strategy design, and the Asana contract text as PLAN_P10.md section 5 describes.
2. Budget: build at B = 16,384. The integrated whole-body measurement decides: if 32 seats miss p95 4 ms / max 5 ms, lower B (12,288 next) or reduce other measured body work; never relabel.
3. Colossal: RAISE the shared cap for the dense shape with exact int16 per-seat weights (option B). Set the cap above the measured full total (chains, buckets, descriptor pool, allocator overhead), rounded, and state the number in the report. James adds: colossal must also be made efficient, so report colossal per-query cost and activation time with the new shape and list the concrete follow-ups that would reduce them (no need to implement beyond what the gates require).

Execute PLAN_P10.md P10.1 and P10.2 in order: integrate the winner into production (body_route_query/body_nav/episode/body), bounded per-seat state only, danger-generation restart rule, determinism by stable seat order, the hierarchical index retained only for legality/steering/hints/zone_safe_ground/coverage with the no-longer-needed following fields deleted (state bytes), delete the legacy planner/minter/scheduler and the replaced hierarchical following code and their tests, retarget the corpus gate and the Phase-1 liveness laws at the new path, then the integrated inclusive body-slice measurement at 16/32 seats natively (all scheduler scenarios), then GameVersion (claim above whatever main has then; run tools/ci/check_gameversion.sh), all nine fixtures, viewer rebuild, docs (design doc + tracked HTML twin, BR_PLAYS.md, FIRST_LIGHT_DEMO.md, AGENTS.md if commands changed), then the canonical linux/amd64 Docker rerun of the full gate, then Phase 11 verification. Merge origin/main at the boundary and say what conflicted. Time rule: focused suites and touched shards during the work; tests/tests.nim once at the end of P10 and once in P11; one isolated containment pair. Phase reports as before. End Phase 10 with PHASE 10 DONE on its own line, then continue into Phase 11 without waiting and end with PHASE 11 DONE.

Claude gates Phase 10 by re-running the corpus gate, the liveness laws, the legacy/new suites, both compile shapes, the containment pair, and by reading the fixture and viewer evidence.
