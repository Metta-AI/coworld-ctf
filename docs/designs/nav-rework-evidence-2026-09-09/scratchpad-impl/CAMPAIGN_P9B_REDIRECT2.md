P9B REDIRECT 2 (after round 1). Claude read the scoreboard. Round 1c/1d prove the room hierarchy was not the binding limit: a complete global weighted 8 px A* is still at carrier-danger-near p95 19.6 % / max 84.8 %. Two hypotheses, test both before any more variants:

H1 — resolution/legality, not search. The oracle's 4 px lattice can stand 7 px from a wall; an 8 px cell is walkable only if its CENTRE has clearance > 6, so every cell whose centre is within 6 px of a wall is dead ground for us. Wall-hugging lanes are exactly where line-of-sight shadows are, so the oracle's cheap detours run through cells we cannot enter. Measure: for the 30 worst cases, what fraction of the oracle route's 4 px points lie in 8 px cells that are NOT walkable (centre-based), and what is the average clearance at those points. Then run the same global weighted A* on the 4 px LATTICE (measurement tool only, shared stamped workspace sized to the lattice, same cost terms, same exact connectors) and report inflation per stratum, pops, and per-query time. If 4 px collapses the inflation to the no-danger numbers, H1 is confirmed and the fix is resolution near walls, not algorithm.

H1 remedies to measure if confirmed, in this order:
 (a) Sub-cell anchors: give each 8 px cell an anchor point = the standable 4 px sub-lattice point nearest its centre (or, better, the one maximising clearance); cells whose centre is unwalkable but which contain a standable sub-point become walkable at that anchor. legalMoves is rebuilt with exact segmentClear between anchors, static lengths use true anchor distances, danger still samples the containing cell. This reclaims the wall-adjacent band at 1 byte per cell. Re-run the global 8 px A* on anchors and report inflation and cost.
 (b) Mixed resolution: 8 px grid with 4 px sub-nodes only in cells within 2 cells of a wall (bounded, sparse), searched by the same A*.
 (c) Full 4 px lattice A* with the shared workspace, if (a)/(b) are not enough; report the workspace bytes on pool maps and colossal (the 32 MiB colossal shared budget is the constraint; count what the route index would no longer need if the 4 px search became primary).

H2 — per-pop cost. 32k pops in 8.5 ms is ~265 ns per pop; the old 4 px planner ran ~30-50 ns per work unit. Profile one expansion: how much is danger sampling (float32 read + quantise), hazard pricing (ETA division), the Q4 int64 arithmetic, heap ops, the legality bitmap read, the stamp check. Then: precompute per-seat per-danger-generation integer cell weights once (an int16/int32 per nav cell per seat, refreshed at the 32-tick danger rebuild, bounded and counted; the per-seat danger raster is already that size), replace the per-neighbour hazard division with a per-cell table when the overlay is dark or with an incremental ETA counter when armed, and use a binary heap with a plain int64 key. Target <= 60 ns per pop. Report before/after pops and ns.

Keep every experiment on its own revertible commit with a scoreboard row; do not touch production caps, the containment gate, or the corpus. End the round with P9B ROUND 2 DONE.
