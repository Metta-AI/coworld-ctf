PROCEED P8, preceded by one mandatory fix-up. Claude verified Phase 7: rework 11/11 (three laws green, unchanged assertions), nav 20/20, seat 27/27, query 10/10, ladder 28/28, episode 15/15, hazard 17/17, both server shapes; containment pair under identical heavy load (load avg 9-14 from another session's suite run): origin/main 6,579 us vs HEAD 5,868 us, so no HEAD regression. Phase 7 accepted at 2c46b6ac.

FIX-UP FIRST (own checkpoint, before any Phase 8 work): src/ctf/server.nim:5222 passes the raw `sim.tickCount - sim.gameStartTick` into `shellEpisode.step` as the elapsed zone tick. `gameStartTick` is -1 until `startGame` enters Playing, so in the lobby that value reads tickCount+1 (whole board "painted") and jumps back to 0 at game start. This is exactly the clock-offset bug class the nav handoff task (Asana 1218200004252283) warned about, and main already fixed the neighbouring call sites in c661fd25 with the guarded `sim.gameTicksElapsed()` helper. Use that helper at the step seam, add a test in tests/test_shell_episode.nim (or the server test) that steps the shell during the lobby (gameStartTick = -1) and asserts the elapsed zone tick the episode sees is 0 and the safe cache is not re-keyed at startGame, and grep that no raw `tickCount - gameStartTick` remains in the shell-facing seams. Report it in a "Fix-up" section of PHASE_7_REPORT.md.

TIME BUDGET RULE from here on: do NOT run tests/tests.nim (the full release suite) inside the fix-up or Phase 8's implementation loop. Run only the focused suites and the shards you touched. The full suite runs exactly once as the last step of Phase 8 (its regression matrix) and once in Phase 11. Containment: one isolated pair per phase, no retries; if both sides fail under load, report both numbers and the load average, do not iterate.

Phase 8 reminders (the regression matrix): every typed route failure; danger+blocked+hazard combinations; ramp/bucket/lifecycle boundaries; literal/class wire strictness; canonical JSON and binary reserved-field goldens; SDK dark/light; death/respawn/inactive; query transactionality; diagnostics for all five states and the new FirstLightNavSummary/formatNavSummary shape; assert no active tick invokes/schedules legacy full-grid planning and no per-seat route-query field scales with map or fine-node count; explicit checks that cover selection/scoring, combat, fog, strategy, drop and handoff are unchanged; docs/designs/FIRST_LIGHT_DEMO.md updated for the diagnostic shape. All four shards green in both compile shapes. NOTE: another session is landing cover-hold on main (lazy default cover from fresh threats, touching episode.nim/default facts); merge origin/main at the Phase 8 boundary and say what conflicted. End with the literal line PHASE 8 DONE.
