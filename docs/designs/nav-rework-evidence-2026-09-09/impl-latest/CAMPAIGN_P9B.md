P9B OPTIMISATION CAMPAIGN (starts after P9 INVESTIGATION DONE; fold its findings in).

James's direction, verbatim in spirit: room navigation must be danger-sensitive; use Fluffy to profile navigation; pursue per-room focused weighted navigation; spitball and try anything that raises path accuracy or lowers nav compute. The gate numbers are the target (per stratum p95 <= 3.0 %, max <= 10.0 %, zero missing; inclusive body slice p95 <= 4 ms / max <= 5 ms at 16 and 32 seats), but this is a campaign of measured experiments, not a single shot. Keep determinism, boundedness (no map-sized per-seat arrays, no full-board search on a play tick) and legality; everything else is open.

Profiling (do this first, keep it):
- Add Fluffy markers on the game thread for every nav stage: query.attach, query.sameRoom, query.sideAstar, query.memoFill, query.hazard, query.refine (new), query.validate, query.install, follow.waypoint, steer, danger.rebuild, safeCache.refresh. Follow AGENTS.md "Profiling build": `-d:ProfileTracePath=<path> -d:ProfileTicks=N`; get one trace from `tools/run_shell_demo.sh` with SHELL_EXTRA_NIM_FLAGS, and make tools/bench_body_nav_rework.nim emit the same per-stage breakdown (ns and counts: pops, arcs, cells read, segmentClear calls) per query so we do not need a server run for every experiment. Markers stay in production if they cost nothing measurable; otherwise behind the profile define like the rest of the codebase.

Scoreboard: $S/impl/P9B_SCOREBOARD.md, one row per experiment: commit, description, overall p95/max, worst stratum p95/max, missing, per-query p50/p95 us (median case / far carrier-danger case), 16-seat and 32-seat first_goals p95 ms (native), retained bytes delta. Iterate on native arm64 with a fast corpus subset (every 8th case plus ALL cases of the failing danger strata and the 4 overflows); run the full 3,072 natively when a candidate looks like a winner; canonical Docker only at the end of the campaign.

Ideas to try, roughly in order of expected payoff (measure each; keep winners, revert losers on their own commits):
1. Hot-room refinement. At each seat's danger rebuild (every 32 ticks) also compute a per-room danger summary (max and sum of danger over the room's cells: one pass over nav cells, bounded, per seat). A route's rooms with zero danger keep their static segments; only HOT rooms (and the endpoint rooms) get a bounded weighted 8 px A* over legalMoves between the entry and exit portal anchors, with the precomputed next-hop distance to the exit anchor as the heuristic (it is the exact static geodesic, admissible for weighted cost). Fixed pop cap per room (try 4,096 / 8,192 / 16,384); on cap hit keep the static segment.
2. Per-seat danger memo keyed by danger generation instead of a per-query memo: arcs x 4 B per seat (9 KB on pool maps, ~150 KB colossal, bounded, not map-sized), so repeated queries within a generation pay nothing.
3. Side-graph scoring from room summaries: score an arc as static length x (1 + w x meanDanger(room)) as a cheap first pass, exact cell sums only for arcs the A* actually settles.
4. Query budget per tick: at most K route constructions per tick (try K = 4 and 8) in deterministic seat order; seats past the budget steer (they already move) and query on a later tick. Measure first_goals bursts under the budget and the time-to-route for the last seat.
5. Follower/geometry inflation on the no-danger and blocked strata (3.36 % p95): try smoothing lookahead K = 8 and 12, allow smoothing across leg boundaries, and string-pull the assembled descriptor once at install with exact segmentClear (bounded by descriptor length).
6. Descriptor overflows: find which cap the four br-gen-23457 cases hit and why (a goal leg longer than 512 cells in a huge room?), then stream that leg from the next-hop field instead of copying it, or split it; do not just raise the cap silently.
7. Incremental re-refinement: when a seat's danger rebuild changes a hot room's summary, re-refine only that room of the current route rather than re-querying.
8. Anything else you think of: say what and why in the scoreboard row.

Rounds: after each substantive round (a profile, or an idea measured), append to the scoreboard, write/append $S/impl/P9B_REPORT.md, and end your reply with the literal line P9B ROUND <n> DONE, then continue with the next idea unless Claude sends STOP or a redirect. Keep machine load in mind (another session is active): no tests/tests.nim runs, at most two parallel compiles. Commit every experiment as its own checkpoint with the scoreboard row in the message.
