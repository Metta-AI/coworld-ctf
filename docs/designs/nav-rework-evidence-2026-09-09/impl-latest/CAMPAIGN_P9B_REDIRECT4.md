P9B REDIRECT 4 (after round 3). Claude read R3.1-R3.4. Accepted: R3.3 (danger>0 dilate-1 + wall band, cold anchors contracted over exact 4 px chains) is the algorithmic winner and passes every quality gate. Also accepted: the 60 ns/pop target was Claude's unsupported guess; your legacy measurement (2.1 us/unit) stands. The binding constraint is now throughput per tick, so round 4 attacks pops per query, ns per pop, and how the work is scheduled. Three levers, each its own commit and scoreboard row, measured on R3.3's graph:

R4.1 Fewer pops: ALT landmark heuristic. At index build, pick L landmarks deterministically (farthest-point sampling over walkable 8 px cells, L = 4, 8, 16) and store one uint16 static geodesic distance (in cells, over legalMoves) per landmark per nav cell: L x navCells x 2 B (L=8: 683 KB pool, 6.2 MB colossal). Heuristic h(n) = max over landmarks of |d(L,n) - d(L,goal)| (in Q4, admissible for weights >= 1), combined with octile by max. Report pops p50/p95/max and ns per query before/after for L = 4/8/16, plus retained bytes and build time. Also try a corridor-with-margin restriction (rooms of the hierarchical route plus every adjacent room) as an independent pop reducer, since D3's failure was zero-margin.

R4.2 Cheaper pops: replace the binary heap with a monotone bucket queue (Dial) over integer Q4 f-values (bucket width 1 Q4 or 16 Q4; circular array sized by max edge cost x 8; keep tie order deterministic by insertion sequence). Combine the per-cell weight and hazard tables into one int32 per cell per seat (single read per neighbour). Report ns/pop p50/p95 for both cases.

R4.3 Scheduling: resumable budgeted search with steering covering the wait. One shared workspace (or K = 2 workspaces if it helps), a FIFO of pending seat requests in stable seat order, and a per-tick POP budget B (try B = 4,096 / 8,192 / 12,288, i.e. roughly 1.4 / 2.7 / 4 ms at today's ns/pop). The head search runs until its budget is spent or it completes; completion installs the descriptor atomically; deferred and in-flight seats steer toward the goal (the production steering primitive) until their route lands. Danger-generation change during a suspended search: restart that search (measure how often). Measure: navigation slice p50/p95/max per tick at 16 and 32 seats for the first-goal burst, moving-goal replans (12-tick cadence) and stuck replans; time-to-route distribution in ticks (p50/p95/max) per scenario; and the fraction of ticks where a seat is steering rather than following. Determinism: identical results across repeated runs and seat-order permutations (stable seat order decides the FIFO).

R4.4 Combine the winners of R4.1-R4.3 and report the same rows, plus the route-quality corpus rerun to prove the heuristic/queue changes did not move inflation (they must not: ALT is admissible and the bucket queue is exact for integer costs).

Note on colossal: per-seat int32 tables at 4 px (1.56 MB/seat) x 32 seats and the 37 MB dense workspace exceed the 32 MiB shared budget on colossal; measure and report honestly, and list the smallest retained set that would fit (e.g. int16 weights, 8 px-resolution per-seat weights sampled for fine nodes, dropping portal next-hop fields if hierarchical following is no longer the primary path). James decides the colossal budget later; do not change the caps.

End with P9B ROUND 4 DONE on its own line.
