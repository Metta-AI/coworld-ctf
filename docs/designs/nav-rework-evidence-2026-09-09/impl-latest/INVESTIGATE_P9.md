P9 INVESTIGATION (no gate changes, no deletions, no Phase 10). Claude has read PHASE_9_REPORT.md. Two questions must be answered with measurements before James decides how to proceed. Write $S/impl/P9_INVESTIGATION.md and end with the literal line P9 INVESTIGATION DONE.

1. Per-query cost is pathological, not merely over budget. 16-seat first_goals p95 = 611 ms canonical / 854 ms native means roughly 38-53 ms per query, while the design estimate was tens of microseconds. Profile ONE query (native arm64, release) on br-gen-22010 room 14 far route and on a median case, with a breakdown by stage: endpoint attach (ring scan + exact connectors), same-room candidates (straight line, via-portal chain walks, local A* pops), side-graph A* (pops, arcs relaxed, memo fills and the cell reads inside them), hazard pricing, descriptor assembly + validateBodyRoute, install copy. Report counts (pops, arcs, cells read, segmentClear calls) and nanoseconds per stage. State whether the harness's timed window includes anything that is not query/follow/steer work (activation, oracle scoring, corpus loading, danger rebuild for all seats). If a stage is O(map) or is repeated per seat when it should be shared, name it. If you find a bug (e.g. the memo being recomputed for every arc on every query, validate re-walking pixels, the local A* using pixel segmentClear per neighbour instead of legalMoves), fix it on a checkpoint commit and rerun the 16-seat first_goals row natively; report before/after.

2. Where does the route-quality gap come from? For the 30 worst cases by inflation (take from the JSON artifact) and the 4 descriptor overflows: (a) does the float-oracle route traverse the same room sequence as ours, or different rooms? (b) how much of the excess weighted cost is incurred inside rooms our route shares with the oracle (intra-room danger crossing) versus from a different room choice? (c) for the overflow cases, which cap overflowed (edge count, start/local leg 512, goal leg 512) and what the actual length was. Give a table. Then, as a bounded EXPERIMENT on a separate commit that can be reverted: corridor-restricted weighted 8 px A* over legalMoves, limited to the cells of the rooms on the chosen side-graph route (plus the endpoint rooms), with a fixed pop cap (try 8,192 and 16,384), used to replace the static intra-room segments when it reaches the goal; report inflation p95/max per stratum and per-query cost with the same profile breakdown. Do not touch the containment gate, caps in production code, or the corpus.

Constraints: no tests/tests.nim runs; keep the machine load in mind (another session is active); commit experiments on clearly labelled checkpoints so they can be reverted.
