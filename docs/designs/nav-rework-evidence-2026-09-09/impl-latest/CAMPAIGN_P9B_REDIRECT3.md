P9B REDIRECT 3 (after round 2). Claude read rows 2a-2d and D1-D3. Read of the evidence:
- 2a proves the target is reachable: the 4 px search IS the oracle (0.000 %). Everything now is about making 4 px-quality search cheap enough, not about finding another algorithm.
- D1/D2 (James's idea) are the best quality-per-pop: overall p95 0.76 %, worst stratum carrier-danger-far 5.4 % / 8.2 %. The remaining 83 % max is the legality gap, not danger: the oracle's cheap lane runs through COLD cells (line-of-sight shadow means danger = 0) that are only walkable at 4 px, so "fine nodes in hot cells only" never gets to use them. 2c (fine nodes in the wall band) fixes exactly that class but was measured alone.
- The time problem is two-fold and both parts are fixable: lazy exact legality per fine edge (D1 far = 198 ms), and ~290-345 ns per pop (2d) versus the legacy planner's ~30-60 ns per work unit on the same maps.

Round 3, in this order, each on its own commit with a scoreboard row:

R3.1 Precomputed 4 px legality. At index build, compute an 8-bit legal-move mask for EVERY 4 px lattice point (standable point, neighbour standable, exact segmentClear between them, no corner cutting), stored once, shared: 4 x navCells bytes = ~171 KB on pool maps, ~1.6 MB colossal. Also a 4 px "standable" bit. Report build time and bytes. From now on no search path may call segmentClear per edge; legality is a table read at both resolutions.

R3.2 Per-pop cost, measured against the legacy planner. Build the legacy BodyPlanner (still in the tree) on the same case and count its work units and ns per unit for the far and median cases, so we have the real target. Then rebuild the 4 px search inner loop to match: dense stamped arrays indexed by lattice index (g int32 in Q4, parent int32, stamp uint32; no hash table), a binary heap keyed by a single int64 (f << 20 | tie), per-seat per-generation integer cell weights (the H2 table, 171 KB/seat pool) read once per neighbour, hazard price from a per-cell table when the overlay is dark and an incremental ETA counter when armed, octile heuristic in integer Q4, no float anywhere in the loop, no Option, no closures. Report ns/pop before/after; target <= 60 ns. Workspace bytes: report for pool and colossal (colossal is over the 32 MiB shared budget only if we keep everything; list what the route index would no longer need to retain if the 4 px search is primary, e.g. portal next-hop fields, and the bytes that frees).

R3.3 Combine D + wall band on top of R3.1/R3.2: fine (4 px) nodes in hot cells (danger > 0, dilated 1 cell) AND in wall-band cells (any cell whose centre is unwalkable but which contains a standable 4 px point, plus their neighbours within 1 cell); 8 px anchors everywhere else. Report inflation per stratum, pops, ns per query, hot+band node counts. Also run the plain full-4 px search with the R3.2 loop for comparison: if full 4 px is fast enough (<= ~2 ms median, <= ~5 ms far), the mixed graph may not be worth its complexity; say which you would ship and why.

R3.4 Per-tick query budget (Idea 0b) on the winner: K = 2/4/8 route constructions per tick in seat order, over-budget seats steer; measure body-slice p95/max at 16/32 seats natively and worst time-to-route in ticks.

No production caps/containment/corpus changes; measurement tools and revertible commits only until a winner is clear. End with P9B ROUND 3 DONE on its own line.
