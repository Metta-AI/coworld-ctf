PROCEED P9. Claude verified Phase 8 with parallel re-runs: rework 12/12, query 12/12, index 7/7, hazard 17/17, seat 30/30, nav 20/20, episode 16/16, ladder 31/31, emit_validator 14/14, view 21/21, binary_view 10/10, play_sdk 3/3, default_play 6/6, standing_order 4/4, server_seam 19/19 (lobby clock stays zero across the start boundary), both server compile shapes. Phase 8 accepted at dafe63a7.

Environment facts for the canonical run: Docker is OrbStack (server linux/arm64, 29.4.0) with linux/amd64 emulation available (`docker run --platform linux/amd64 alpine uname -m` prints x86_64), 14 host cores. So the canonical BUILD (linux/amd64, Nim 2.2.4 from the repo Dockerfile, -d:release -d:useMalloc --threads:on --opt:speed) and the corpus/activation/memory gates CAN run here for real. Absolute tick-latency numbers under emulation are provisional and must be labelled so; still run them (16 and 32 seats, all six scenarios, 5 warmups / 30 reps, --cpus=1) and report p95/max next to the arm64 native numbers, and state the ratio between the two builds' BodyMap baselines so a reader can scale.

Phase 9 reminders: harness tools/bench_body_nav_rework.nim emits machine-readable JSON (commit, dirty state, NimVersion, OS/arch, flags, image id, cpu limit, pool digest, per-case samples, thresholds, pass/fail); refuse a dirty tree or wrong arch for the canonical rows. Route quality over all 3,072 corpus cases against the float oracle: p95 <= 3.0 %, max <= 10.0 % overall AND per roster/profile/dynamic/distance stratum, with count/missing/illegal columns; a missing or illegal route is a failure. Activation: newBodyMap alone vs map + index (+ overlay when armed) for all 64 published specs and colossal, same process/image, 1 warmup + 5 samples pool / 3 colossal; <= 2x pool, <= 3x colossal; transient peak separately. Retained memory: exact accounting per map <= 16 MiB pool / <= 32 MiB colossal; per-seat danger rasters reported separately. Tick time: inclusive body slice via bodySliceNanoseconds, hard cases including cold memo and worst-degree rooms named literally. Hash the corpus routes on arm64 native AND the amd64 canonical build and report whether they match (they must). If any gate fails: report the exact rows and apply only the approved structural fallbacks (segment sampling, bounded memo-fill degradation, structural alternatives, earliest-arrival label); never raise live caps or remove cases. Commit the harness/instrumentation and any separately reviewed structural fix. Time budget: no tests/tests.nim runs in this phase. End with the literal line PHASE 9 DONE.
