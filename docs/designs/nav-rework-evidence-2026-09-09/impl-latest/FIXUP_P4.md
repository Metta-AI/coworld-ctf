P4 FIXUP required before Phase 5 (two items).

1. Layering: `src/ctf/zone_field.nim` now imports `../shell/body_hazard`, which imports body_route_index and body_map. zone_field is sim source compiled into the wasm replay viewer (Dockerfile.replay-viewer builds replay-viewer/ctf_replay.nim with -d:emscripten); the design and PR #408 rely on NO body_* module being in that build, and ctf must not depend on shell. Invert it: define `ZoneDamageSnapshot` (grid dims, cell px, fingerprint, damage seq) in the ctf layer (zone_field.nim itself, or a tiny dependency-free ctf module) and have body_hazard import it. zone_field must import nothing from src/shell. Prove it: `grep -rn "shell/" src/ctf/` returns nothing new, and compile replay-viewer/ctf_replay.nim with `nim check -d:emscripten` (or the exact Dockerfile command if a local emscripten is absent, then say so and add a unit test that fails if zone_field imports shell — e.g. a test that `nim check`s zone_field with --path restricted to src/ctf).

2. Tick-work delta: your containment table shows 11 of 12 waves slower than the adjacent baseline (+152..+567 us, verdict +4.4%), whereas the Phase 3 fixup table had mixed signs. Something on the per-tick path grew. Measure it (36-sample diagnostic timings as you did for the index constructor) for: the hazard/danger memo stamp checks, any per-tick overlay/elapsed-tick bookkeeping in episode.step or seatTick, BodyNavSeat growth, and the safe-cache rekey path. Ticks that never call the new query path must cost what they cost at origin/main. Fix, then show three back-to-back baseline/HEAD pairs; accept only when the per-wave deltas are mixed-sign or the residual is explained by a measured cause you cannot remove without touching the legacy path (say which).

Commit as checkpoints, add a "Fixup" section to PHASE_4_REPORT.md, end with the literal line P4 FIXUP DONE. Do not start Phase 5.
