PROCEED P7. Claude verified: emit_validator 13/13, body_seat 26/26, nav 20/20, view 21/21, binary_view 10/10, play_sdk 3/3, hazard 17/17, episode_ladder 25/25, both server shapes, containment 4,683 us pass. Phase 6 accepted at 73a60cc6.

Phase 7 is the phase that changes movement. Reminders: the bounded route follower drives the mask (copied start/local and goal legs, index-read middle segments and crossings, exact endpoint connector); follow-time smoothing K = 6 with the exact pixel predicate from the cog's real position; an arrived endpoint is never an advancing route; deterministic eight-octant steering in NavNeighbors order whenever no advancing route exists, integer score, first octant wins ties, `bnsNoProgress` when nothing progresses; `bnsStalePath` removed and the diagnostic enum becomes idle/following/steering/no-progress/arrived; the death clearing order from PLAN.md section Phase 7 runs BEFORE setSeatActive(false), and inactive seats are asserted never queried; respawn starts with no route-life state. The three Phase-1 laws in tests/test_shell_body_nav_rework.nim must go green WITHOUT changing their assertions, then the file is wired into the fastest shard (measure first). The legacy planner remains compiled and its suites green until Phase 10, but from this phase the new path owns movement for play seats; say explicitly in the report which path produces masks in production and in each test. Include the isolated containment pair and a FIRST LIGHT run showing a cog moving on its first navigate tick. End with the literal line PHASE 7 DONE.
