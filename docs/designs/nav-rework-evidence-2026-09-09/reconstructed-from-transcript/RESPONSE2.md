# Round 2 response: all seven corrections accepted

1. Arithmetic. Search costs 128/181 with the exact integer octile heuristic 181*min(dx,dy) + 128*(max-min) between cell anchors. Portal fields store ONE uint8 next-hop per (room, portal, room cell) (halves memory: colossal 9.5 MB); distances to a portal are obtained by walking the chain, which exact scoring does anyway. Segment static length uint32 in 1/16 px; live totals checked int64; round-to-nearest stated per rational product. The cost-gate oracle stays the existing 4 px float formula; the search's danger quantisation (<= 0.49 % relative on a carrier path) is reported against that unquantised oracle.
2. Graph nodes = (portal, incident room) sides; intra-room segment edges between sides; one crossing edge per portal. Seat route = ordered edge IDs with direction, fixed 4,096-entry descriptor buffer, asserted at activation against the longest simple path bound.
3. Route descriptor assembled and validated in shared query scratch (edge IDs, copied start/local leg, copied goal leg, exact connectors), then copied into per-seat fixed buffers and the revision flipped: a failed query never touches the active route. Local legs bounded by the search cap, not room size. Segment danger memo becomes a SHARED query-local memo keyed by edge ID (27,460 x 4 B = 110 KB shared, cleared by generation per query); no per-seat memo.
4. Local A* cap is a latency-derived constant (4,096 starting hypothesis); on cap hit the legal portal candidate wins and its full inflation counts in the stratum; failing the 10 % max means adding structural alternatives to the index, never raising the playing-tick cap.
5. No origin snapshot: the overlay is schedule-relative and origin-free; the server passes the live elapsed zone tick into the FirstLight step; BodySafeCache re-keys on game generation or non-monotone elapsed time. (Verified: gameStartTick is -1 until startGame.)
6. Subtask 02 gains an exhaustive offline coverage proof: every standable pixel of every supported map has a legal <= 32 px connector to an anchored node of the same pixel component, and anchored-node graph connectivity equals pixel-component equality; failure builds the sparse 4 px micro-corridor nodes before qualification. The route corpus is the cost test only.
7. Per-room maxDamageArrival at overlay build; bucket roll updates room flags in O(rooms). Edge hazard pricing advances the ETA cell by cell along the segment at 2.0625 px/tick in traversal order; extrema only skip provably zero or provably saturated segments.

Round 3 request: write collab/VERDICT2.md with `VERDICT: GO` or `VERDICT: REVISE` (numbered, specific). End with the literal line VERDICT2 DONE.
