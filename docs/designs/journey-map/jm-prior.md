<!-- Source: ~/.ctf/knowledge/stranger-walk/journey-map/jm-prior.md, copied verbatim 2026-09-09 (shot paths repointed to ./shots/; jm-prior itself has no shot citations) -->

# jm-prior — prior-art digest for THE WHOLE journey map (era: 2026-09-09, live paintbot-v0.7.369/GV61/Glory16, coworld-ctf main ≈ 7353e5ae, one commit past 070d4805)

Digest only — read this, not the sources. All items read directly from origin/main, named branches, PR bodies, or live curls on 2026-09-09.

## Repo docs (docs/designs, docs/wiki, docs/*)

**FRONT_DOOR.md** (`docs/designs/FRONT_DOOR.md`, merged #495 `c495f519`) — Entry-URL truth table (12 rows, /paintbot → submission) and the doorstep bug: the page's single most on-the-nose CTA, right-rail "Compete → Submit a policy," hrefs literally `/#production`, a dead anchor since the homepage redesign (#21018) deleted its target — clicking it lands on the unrelated marketing homepage, no Paintbot context survives. Proposes a one-line diff (`href={leagueRoute(league.id)}`, reusing the already-working "Join the League" pattern) for a metta engineer to apply; not opened as a metta PR (repo boundary). Also ships 3 hook-sentence candidates + a static mock (`front-door/index.html`, title "Paintbot — front door mock (Candidate 1)"), Candidate 1 recommended and used verbatim in metta PR #22190 below. **KEEP:** the entry-URL table, the doorstep-bug diagnosis+diff, Candidate 1 copy. **CONTRADICTS:** #22190 ships Candidate 1's hook/loop/CTA but does NOT touch the broken `/#production` href (different render location) — the doorstep bug is likely still live. **Verdict: design input**, and the href fix is still unshipped anywhere.

**BUILDER_DOOR.md** (`docs/designs/BUILDER_DOOR.md`, merged #493 `cbb8bcdd`) + its 5 research dimensions (`docs/designs/builder-door/01-05`, same PR) — Recommends **Rank 1 now** (fix the existing template-repo door: surface the working `--run` invocation, fold `--platform linux/amd64` into every doc'd `docker build`, add a protocol quick-ref) **then Rank 2 next session** (a `coworld init paintbot` CLI wizard, if it specifically absorbs the `--run`/`--platform` traps). Ranks a browser editor (4) worst — Paintbot's real artifact is a linux/amd64 container image, not a function a runtime executes directly. Cross-platform finding: **no reference platform (14 researched) publishes a time-to-first-submission metric**, and none has a genuinely interactive CLI wizard as its onboarding tool — a wizard here would be a reasoned bet, not a precedent match. **KEEP:** Rank 1's punchlist (already executed by wiki PR #499, see below), the "no wizard precedent in this genre" finding, the DX-evidence bottom line (CRA/create-next-app chose wizards for cognitive-load reasons, never A/B-tested vs templates; Stripe's DX reputation has zero primary numeric backing). **CONTRADICTS:** nothing direct; **candidate-only** on whether to ever build the wizard (Rank 2 unbuilt, no `coworld init` exists — grepped, zero hits). **Verdict: design input** (Rank 1 already partly shipped via #499; Rank 2 open).

**WHY_ONE.md** (`docs/designs/WHY_ONE.md`, merged #497, `tools/glory/why_one.py`) + `why-one/index.html` mock (h1 "Paintbot (Season 2) — Competition standings," kicker "Mock — v1 deeds-first row, generated from a real decode · not a live surface") — Owner's favorite legibility idea (R10): an expandable standings row under #1 showing which named deeds built the lead, how many rounds it rests on, delta vs #2 — **deeds first (v1, ship-able now)**, brain-style/heat-carry second (v2, needs S3 risk bands, deferred). Real worked example against the live board (pv `6d278f56`, standing 647,002, "rests on 3 rounds"). Ships only a data tool + static mock inside `coworld-ctf`; the real home is **metta's Observatory standings list**, James's lane, not touched here. Load-bearing gap it proves: **the platform can tell you a rank and a score, never a reason** — no server "why" endpoint exists. **KEEP:** the row content spec, the "never a bare number" rule, the nav-is-James's-not-ours boundary. **CONTRADICTS:** none. **Verdict: design input**, blocked on metta wiring (`PLATFORM_LEGIBILITY_DATA.md` scopes the fix, below).

**ENDCARD_V1_STANDINGS_DELTA.md** (`docs/designs/ENDCARD_V1_STANDINGS_DELTA.md`, merged #505 `588f1ac7`) — Owner wants endcard v1 = each seat's season-standing delta from the episode just played. **Verdict: NOT buildable, not even live-only** — three stacked gaps: (1) the wasm client has zero outbound API capability by design (pure wire consumer), (2) no seat→player_id/round_id key reaches the client at all (identity is attached server-side only, never echoed to replay/client), (3) `recent_rounds` exists in the leaderboard schema but is `null` on every entry — no "standing before/after round N" anywhere in the public API. Doc-only PR, no client/bundle changes. **KEEP:** the 3-gap diagnosis, the fix options (populate `recent_rounds`, or a new deltas endpoint) — now scoped concretely by `PLATFORM_LEGIBILITY_DATA.md`. **CONTRADICTS:** the task brief's assumption "v1 may be LIVE-only" — it's worse, missing plumbing not just missing data. **Verdict: design input**, hard-blocked on metta.

**STRANGER_WALK.md** (`docs/designs/STRANGER_WALK.md`, merged #492 + extended by #507 `7353e5ae`) — The measurement instrument itself; see closing sections for the prompt text and headline numbers. **KEEP:** M1–M8 milestone definitions, the isolation protocol (v1.1 auto-continue, v1.2 detached launch, v1.3 real container), the finding that M4 (human seat) is unreachable on live S2 slot config (every slot is `control:"play"`; the human seat only works on legacy non-play slots, currently only reachable via the free-play field, which 404s on `/api/field` — **confirmed still 404 today**, see URL table). **CONTRADICTS:** nothing; supersedes its own Protocol v1 (self-reported milestones invalidated as biased/scaffolded — see walk-protocol-v2 below). **Verdict: design input** (THE source of measured evidence for the whole epic).

**docs/wiki/AUDIT.md** (merged as part of #496 `df7c844d`) — Every wiki page except 4 is stamped "Verified against GV24/Glory 12" — three eras stale. No `battle-royale-s2` page existed before this task (7 pages red-link to it). Found a real **regression** (not just staleness): `ranks.md` says grenade-charges buffs are "dead code," but GV61 wired all six `levelX` buffs live (rank-4+ grenade genuinely yields two throws now) — the wiki says weaker than truth. Also found `modes.md` **self-contradicts** its own changelog (says duo pairing still applies; the wiki's own changelog entry says solo since 2026-09-05) — **not yet fixed** (deletion listed as PUBLISH.md step 4, "optional, do last," unexecuted). A platform-side defect too: `game.description` via `coworld leagues` still says "sixteen duos." **KEEP:** the staleness table, the regression finding, 27-pages-unaudited flag. **CONTRADICTS:** `modes.md`'s own two sections vs its own changelog. **Verdict: design input**, wiki lane's own open punchlist.

**docs/wiki/PUBLISH.md** (same PR family, procedure doc) — Mechanical curl-only PUT procedure (Python `urllib` gets Cloudflare 403), rate-limited 30 rev/60s, for anyone with wiki write access. Steps: publish `battle-royale-s2` + `build-and-submit` (done, see below), repoint 7 `[[battle-royale]]` red links (done per AUDIT.md's changelog), delete `modes`'s two stale sections + `submitting-a-policy`'s unverified-platform-push section (**not done** — explicitly deferred). **Verdict: candidate only** (a runbook, not a design decision) — flags that wiki cleanup is partially, not fully, executed.

**docs/wiki/RESTRUCTURE-PLAN.md** (merged #496) — Reorders the wiki into an 8-chapter "book" matching the stranger's own question ladder (what is this → how it works → S2 rules → scoring → build → run locally → submit → read the ladder); explicitly does NOT rename any slug. **KEEP:** the chapter mapping, the explicit refusal to create a bare `battle-royale` stub (avoids repeating a "wrong-page link" failure the wiki already paid for once). **CONTRADICTS:** nothing; leaves `glory-season-2`'s full re-verification and the 27-unaudited-pages sweep as explicit follow-ups, still open. **Verdict: design input**.

**docs/wiki/build-and-submit.md** (NEW page, merged #499 `29279948`, era-refreshed #502 `0fe41200`) — The wiki's first real, verified, end-to-end "download → run locally → package → upload → submit" chapter, with both undocumented CLI traps reproduced firsthand (`--variant` silently defaulting to the certification fixture, not the real ladder ruleset; `--run` needing one token per flag, never JSON). **KEEP:** as the canonical build-and-submit reference; already implements Builder-Door Rank 1's punchlist. **CONTRADICTS:** none. **Verdict: design input**, already live (confirmed 200, see URL table).

**docs/wiki/battle-royale-s2.md** (NEW page, merged #496) — The first real ruleset page for the live variant: 16 solo seats (not duos), 4 HP, closing-rectangle zone with a published phase schedule, downed state armed but scoring-neutral for a solo seat (no teammate to revive), rank buffs now wired live. Explicitly frames every stat as **live-service config, re-read before citing**, not an engine constant. **KEEP:** as the ruleset source of truth. **CONTRADICTS:** `modes.md`'s stale duo-framing (see AUDIT.md). **Verdict: design input**, live (confirmed 200).

**docs/PROTOCOL.md** (updated #499) — Deprecated Sprite v1 direct-input protocol, but now leads with a "Season 2 quick reference" pointing at the real play-seat contract (WASM plays by name, not button masks) and the normative spec doc. **KEEP:** the Season 2 quick-reference block, the frame-pacing/dead-reckoning/lobby-detection notes (still true for the underlying transport). **CONTRADICTS:** none — this was the doc the PoC author flagged as "the single highest-leverage thing to fix" (protocol table buried 490 lines into a 3,501-line design doc); now partially fixed. **Verdict: design input**.

**docs/RULES.md** (untouched this era, deprecated banner at top) — Classic 2-team CTF ruleset (8v8, heart-capture, fog-of-war). Explicitly marked deprecated since 0.7.253; Season 2 battle-royale is the live ruleset, documented instead in `battle-royale-s2.md`/`BR_PLAYS.md`. **Verdict: obsolete** for the beginner journey — do not cite for what a stranger needs today; only relevant if the design ever needs to explain "classic mode still exists but isn't scheduled."

**docs/ACTIVE_USERS.md** (untouched, `dau_server.py` tooling) — Proposes one DAU definition for any coworld: **~7 daily active humans on Paintbot**, not the naively-read 18/20/26 (round-fielded/leaderboard-entrant/lifetime-submitter counts) — because policies play unattended forever, so raw activity metrics can't fall even if every human quit. Caps repeat actions per person (3/day) and excludes machine-cadence uploads (median 2.1 min gaps = a bot, not a person). **KEEP:** as the honest denominator for "how many people are on this journey" — relevant context for scoping the redesign's expected audience size. **CONTRADICTS:** none. **Verdict: design input** (background fact, not a UX decision).

## Branches / PRs (coworld-ctf)

**maxwell/walk-protocol-v2** (pushed to origin, **no PR opened**) — Rewrites `prompt.md`/`judge.md`/`score.py`/`smoke_prompt.md`. Owner ruling: Protocol v1 handed the stranger the 8 milestones by name and asked it to self-narrate `MILESTONE:`/`BELIEF:` lines — ruled invalid as a clean-slate discovery test (primes the stranger to hunt for exactly the 8 things being measured). **Protocol v2's stranger prompt is verbatim:**
> "You are a competent developer who has never heard of Paintbot. Starting from {{ENTRY_URL}}, get a policy of your own onto the Paintbot ladder and make it climb. Think aloud as you go. Never post, message, or contact anyone. Never read files outside your working directory except what the public surfaces link to."
(plus mechanics: check working dir for an `env` file with `STRANGER_EMAIL` to self-sign-up; `WAITING:` if blocked on a human-relayed verification code; stop at 3h wall-clock or 30min stuck.) It never mentions milestones/beliefs/blocked/ready. The judge now extracts M1–M8 and belief statements **post-hoc** by reading the raw transcript prose after the fact, not from self-report. **KEEP:** this is now the operative protocol for any future Walk. **CONTRADICTS:** Protocol v1's five completed runs (kept on record, still cite real defects, but invalid as a "did the game explain itself unaided" baseline). **Verdict: design input** — but note this is a *measurement* branch, not a product-copy branch.

**maxwell/explains-itself-inventory = PR #508 OPEN** (`docs/designs/THE_GAME_EXPLAINS_ITSELF.md`) — The single most complete synthesis doc: 12 in-game "moments" (door's first screen, round starting, a tag, a deed POP, the multiplier moving, a pact forming, a downed/rescue, final four, the win, the endcard, the standings page, a rank moving) each scored today/wrong-belief/must-say/data/milestone/effort, then a ranked 12-item PR-sized fix list. Top 8 by priority: `--run` argv fix, kickoff objective banner, strip legend tooltips (pact/heat/multiplier/downed have **zero** aria-label/title anywhere), wiki stale-era sweep, standing-model correction (EMA not max — a real wrong belief opus-a held), why-#1-is-#1 v1, GitHub-only-signin disclosure, doorstep CTA fix. Registers (does not duplicate) `phase2-door`/`phase2-why1`/wiki-lane work. **KEEP:** the whole moment inventory and priority order — this is the closest thing to a pre-existing "holistic design" already done. **CONTRADICTS:** nothing internally; is itself the reconciling document for FRONT_DOOR/WHY_ONE/ENDCARD/wiki. **Verdict: design input — read this one first if you only read one.**

**maxwell/walk-container = PR #507, MERGED, now the tip of origin/main (`7353e5ae`)** — Isolation + credential contract for stranger runs: each run gets its own Linux container (own PID namespace, filesystem, network — closes the `ps aux` process-table leak `run.sh` couldn't close), its own credential via `STRANGER_ANTHROPIC_API_KEY_FILE` (never the host's `~/.claude`), three modes (`probe`/`selftest`/real run). **Verdict: design input** for anyone running Walk 2, not itself product-facing.

**PR #506, MERGED** (`docs/designs/PLATFORM_LEGIBILITY_DATA.md`, already on origin/main) — The metta-side scoping doc for both WHY_ONE and ENDCARD's blockers. Root cause, cited to exact files/lines: `ScoreRankingAlgorithm.leaderboard()` never populates `recent_rounds`, and its only input carries current standing with **no round history at all** — but the live settlement path (`ladders/updater.py`) has both pre- and post-round standing in hand at the exact same call site, every round, for free. Proposes a PR-sized Step 1 (populate `recent_rounds` with before/after standing+rank in `RoundResult.result_metadata`, no wire change, no migration) and an optional Step 2 (server-side EMA-weight summary). Per-deed breakdown (CLOSING×N etc.) explicitly needs a WIRE-OK game-side change, out of scope until the lead calls it. **KEEP:** as the exact spec for the metta worker who picks this up. **CONTRADICTS:** none. **Verdict: design input**, the concrete unblock for two of the epic's best ideas.

## metta (read-only; none of these are our repo's to merge)

**#22190 OPEN** ("softmax.com: paintbot door first screen — hook, loop, Watch-first CTA") — Adds a new hero section above the existing `/paintbot` theater, Paintbot-gated. Hook line verbatim: *"My brain fights other people's brains, and that produces a legible testbed for agent behavior."* One paragraph what/why/how, the loop restated as one line, primary CTA **"Watch a round live"** (anchors to the existing stage), "Join the League" demoted to secondary. This is FRONT_DOOR.md's Candidate 1, built the same day. **Does not touch** the broken `/#production` right-rail CTA (different code path). **Verdict: candidate only** — good copy, unmerged, and even once merged leaves the doorstep bug unfixed.

**#21753 OPEN** ("a real /forum page, a readable Feed, and replies addressed to you") — Ships a real `/forum` route (today: **404**, confirmed live) mirroring the existing coworld-scoped forum components; filters ~8,821 unreadable html/bundle-format "Post"/"Report" rows out of the feed (only ~180 were ever real markdown); adds a `GET /v2/inbox` for replies addressed to you. Not Paintbot-specific but touches the same `ForumFeed`/`SurfaceChrome` primitives `/paintbot/forum` already uses. **Verdict: candidate only**, unmerged; the site-wide forum is not yet part of a stranger's real path.

**#19807 OPEN** ("/watch: the rail is REPLAYS — full mode list, and every row says what kind of game it is") — Renames the /watch rail's implicit label to "REPLAYS" (vs "Highlights"/"Episodes," both explicitly rejected by the owner in review), surfaces `variant_name` per row (mode+map+team-format in one string) only where it varies across the visible window, and labels the sort/filter controls. Relevant precedent for "how do we name a list of matches without inventing a synonym that collides with platform vocabulary" (see vocabulary.md below). **Verdict: candidate only**, general /watch UX, not Paintbot-door-specific.

**#16467 OPEN** ("League Commissioner — setup wizard + League Health dashboard") — A conversational, wizard-style league-creation flow (coworld → seats → cadence → …) that emits one paste-into-Claude-Code prompt to stand up a real league, plus a Simple/Detailed operational-health dashboard. Relevant only as **the one place in the whole codebase a conversational wizard pattern is being built** for a Softmax-adjacent onboarding flow — worth knowing it exists as a style precedent, but it's commissioner-side (running a league), not builder-side (submitting a policy). **Verdict: candidate only**, different persona.

**docs/ai/design/vocabulary.md** — Canonical entity naming, enforced across every ux.* skill: **User → Player → Policy → PolicyVersion**; **Game → Coworld → League → Division → Round → Episode**; **Champion ≠ winner** (means "currently competing"); **Score not "Points"/"VOR"**; **Round is a batch of episodes, never a turn/phase inside one match** (the single most damaging collision — an engine's own within-match "round" must be renamed, e.g. "scene"). A beginner-facing surface must use these terms exactly and actively push back on collisions, not silently adopt off-system language. **Verdict: design input — binding vocabulary constraint** on any copy the holistic design writes.

## Knowledge docs (~/.ctf/knowledge/)

**00-owner-decisions-2026-09-09.md** — Already known to the lead; rulings pulled into the closing section below only where other docs cite them (R7–R11).

**stranger-walk/builder-door-research/00-plan.md** — The zipper plan that produced BUILDER_DOOR.md's 5 dimension docs (identical content to `docs/designs/builder-door/01-05.md`, already digested above — this is the task brief, not new content). **Verdict: obsolete** (superseded by its own output).

**paintbot-entry-design.md + -AMENDMENTS.md + -step0-ledger.md** (dated ~2026-08-26, pre-dates Season 2 solo BR) — A separate, detailed, heavily-cited design for a **human** "walk on and play" entry experience (a hero page, a zero-cost practice cog using the real renderer with `websocket:false`, WASD controls, a queue beat, full-viewport seated play, seat-takeover with safe display names that never reach the cog on the wire). Explicitly **out of scope for THE WHOLE**: owner ruling R7 states *"'Take a seat' is NOT part of this — human play is a separate matter."* Its target surface (`/api/field`, free-play) is the same one STRANGER_WALK.md's M4 finding says currently 404s. **Verdict: obsolete/parked for this epic** — do not re-derive or resurrect without a fresh owner call; cite only if the design ever reopens the human-seat question.

## Owner rulings already made
- **R7** (`00-owner-decisions...md`): first minute must answer, in order, what is this → why would I care → how it works; "why would I care" is make-or-break. "How it works (the loop)" also belongs in the first minute; build/submit wait for a click. Layout mockups were rejected — design from the stranger's questions, not screens. **"Take a seat" is explicitly NOT part of this task** — human play is a separate matter (owns: paintbot-entry-design.md's whole track, descoped).
- **R8**: the hook, verbatim: *"My brain fights other people's brains, and that produces a legible testbed for agent behavior."* Already shipped as copy in metta PR #22190 (unmerged) and `front-door/index.html`.
- **R9**: why the hook is invisible today — behavior is mostly not legible, and the fight of minds is not visible.
- **R10**: legibility priorities — **"WHY #1 IS #1"** (owner's favorite; standings row expands into what earned it) is the top ask. Scoring moments already mostly explain themselves. Pacts MUST be visible (solid or not) but **NO lines drawn on the field**. Intent-on-the-cog = cool but a clutter risk.
- **R11**: where the mind lives — in the **score-bug strip**, not on the field: pact partners grouped side by side with a shared mark; a cog's current intent as one word under its name. The field stays clean.
- **Lane boundaries** (cited repeatedly across FRONT_DOOR.md, WHY_ONE.md, THE_GAME_EXPLAINS_ITSELF.md): nav and the Observatory standings page are **James's** (`ctf-nav-is-james-we-supply`) — coworld-ctf workers hand over specs/mocks, never merge into metta. Campaign is **David's**, out of scope (per standing memory, not re-derived here). The wiki is its own lane (wiki-is-the-book, phase2d).
- **Owner authorized cleanup** (STRANGER_WALK.md): a real credential-leak incident (a smoke test submitted under the owner's own account) was retired via `coworld retire-membership`; the fix (isolated `$HOME` per run) is now standing protocol for any future Walk.
- **season_leg_transform: "none"** confirmed live 2026-09-09 (ENDCARD doc's own probe) — the geometric-mean "typical episode" framing discussed elsewhere in the glory epic is **not armed**; do not write copy describing a "typical vs best" transform as current.

## Entry URL truth (curled live, 2026-09-09; log at `/tmp/jm-prior/urls.log`)
| URL | Status | Notes |
|---|---|---|
| `softmax.com/paintbot` | 200 | Live entry point; the doorstep-bug CTA sits on this page. |
| `softmax.com/paintbot/wiki/main` | 200 | Wiki front page, live. |
| `softmax.com/paintbot/wiki/battle-royale-s2` | 200 | New ruleset page, confirmed published. |
| `softmax.com/paintbot/play` | **404** | Was documented as a working spectate/lobby surface in FRONT_DOOR.md the same day; **now 404s** — matches STRANGER_WALK.md's separate finding that the free-play surface (`/api/field`) is currently broken. Treat as a live-service fact that moved within the day, re-check before citing. |
| `softmax.com/paintbot/forum` | 200 | Coworld-scoped forum, live. |
| `softmax.com/forum` (PR #21753's new sitewide forum) | **404** | Confirms #21753 is unmerged/undeployed. |
| `docs.softmax.com/guides/quickstart` | 200 | Generic cross-game quickstart; correctly punts to `play.md`. |
| `softmax.com/sign-in` | 200 | GitHub-OAuth-only gate (single button, verified live in stranger transcripts). |
| `softmax.com/play.md` | 200 | The real, correct, Paintbot-specific agent-readable instructions — reachable only via the docs.softmax.com detour or guessing the URL today. |
| `softmax.com/observatory`, `/observatory/v2` | 307 → `/sign-in?redirect_url=...` → 200 | Sign-in gated server-side; the "Participate in this League" play-prompt lives 3 clicks deep here. |
| `paintbot.apps.softmax.com` | 404 | Confirmed dead/wrong-app path (matches memory `ctf-paintbot-apps-hosting-path`). |

The wiki's published surface (`softmax.com/paintbot/wiki/*`) answers 200 throughout; no separate "wiki hosting path" exists outside this Observatory-served route.

## Conflicts the design must resolve
- **The door's #1 CTA is still a dead link, and two separate unmerged fixes exist for two different parts of the same problem.** FRONT_DOOR.md's proposed one-line href diff (fixes the actual broken `/#production` anchor) and metta PR #22190 (ships a new hero above the theater, doesn't touch that anchor) both sit unmerged — landing #22190 alone would still leave the doorstep bug live.
- **`/paintbot/play` status disagrees across same-day sources.** FRONT_DOOR.md (written 2026-09-09) calls it a working spectate/lobby surface; a live curl the same day returns 404, matching STRANGER_WALK.md's independent finding that the free-play surface is currently broken. Don't trust either doc's URL table without a fresh check.
- **The wiki `modes.md` page still self-contradicts its own changelog** (says duo pairing current; the wiki's own changelog says solo since 09-05) — flagged by AUDIT.md, fix specified in RESTRUCTURE-PLAN.md and PUBLISH.md step 4, explicitly **not yet executed**.
- **Builder-door and stranger-walk data conflict on how bad GitHub-only sign-in is.** BUILDER_DOOR.md/05-our-evidence.md frame it as "a hard identity wall no docs fix"; STRANGER_WALK.md's actual runs show it was *fully surmountable* once real `GITHUB_USER`/`GITHUB_PASS` existed — meaning the real unmeasured risk is a stranger with **zero** GitHub account at all, not GitHub-OAuth as a mechanism. THE_GAME_EXPLAINS_ITSELF.md's fix #7 (disclose the sign-in requirement) treats it as a documentation gap; nobody has measured the true no-account cost yet.
- **Two different "what should the first screen say" answers ship in two repos.** `front-door/index.html`'s mock and metta PR #22190's actual copy match (Candidate 1), but FRONT_DOOR.md itself offers 3 candidates and only recommends, doesn't rule out, the other two — if the holistic design wants a different hook ordering, #22190 would need to change too, and nothing currently forces that sync.
- **Endcard/standings "why" and "delta" are both blocked on the same unbuilt metta plumbing** (`PLATFORM_LEGIBILITY_DATA.md`), but WHY_ONE.md frames its v1 as "ship-able now" (true only for the offline replay-decode tool + static mock, not a live surface) while ENDCARD_V1 frames its v1 as flatly "NOT buildable" — same root cause, different framing of urgency; the holistic design should present them as one platform ask, not two.
- **Human play (M4) is simultaneously "explicitly out of scope" (R7) and the subject of a large, detailed, unshipped parallel design** (`paintbot-entry-design.md`) that nobody has formally closed — worth one line in the new design confirming it stays parked, so a future worker doesn't rediscover and re-litigate it.

## What I could not read
- Live click-through of the `#production` anchor bug in a real browser (only static `curl`/href inspection available in this pass — FRONT_DOOR.md already did the real browser repro on 2026-09-09).
- metta's actual current `WatchTheater.tsx` source (read-only PR-diff/body only, per the "don't enter other worktrees" boundary — this repo has no metta checkout of its own; `/Users/maxwellstarr/projects/metta` is a sibling checkout on an unrelated branch, read via `gh pr view/diff` and direct file `Read` only, never `cd`/git-state changes).
- `docs/ai/design/observatory-design-spec.md` beyond its color-token section (365 lines; the Terminology content is already fully covered by `vocabulary.md`, which supersedes it as the source of truth per that doc's own header).
